# Validator Lexical-Proxy Audit — Confirmed Defects

Research-only audit of wayne-* validators and eval harnesses. Each finding = a string/shape/mechanism proxy standing in for a semantic judgment it cannot make. All adversarially verified; legit machine-layer checks (hash, schema, state-closure, ID) excluded.

**Totals:** 17 units audited · 98 confirmed · 3 flagged-but-preserve · 21 rejected

| Category | Count |
|---|---|
| LEXICAL-FALSE-POS | 33 |
| LEXICAL-FALSE-NEG | 31 |
| SHAPE-POLICING | 29 |
| MISCLASSIFIED-ORACLE | 5 |

| Severity | Count |
|---|---|
| high | 2 |
| medium | 63 |
| low | 33 |

## Rule: don't gut, split

For every entry: **keep** the machine-layer / real-invariant half named in *Preserve*, **remove or move to model-judgment** only the lexical-proxy half.

## eval-triage (9)

### `check_trial.py:319` — HIGH · SHAPE-POLICING
- **Proxy:** An exact count of '?' characters stands in for the judgment 'the agent asked for the missing data instead of routing'.
- **Code:** `question_marks = output.count("?") + output.count("？") ⏎ if question_marks != 1: ⏎     findings.append(f"missing-data response must ask exactly one question; found={question_marks}")`
- **Concrete failure:** Agent replies 'Please share the path to the failing log and the exact command you ran.' — a perfectly valid single request for the missing input, phrased imperatively with zero question marks → rejected (found=0). Likewise 'Where does the log live? And what command produced it?' is one coherent clarification but has two '?' → rejected.
- **Preserve:** The real invariant (do not route/fabricate when input is absent, ask for it) is separately enforced by the where/how regex at line 321 and the no-route check at line 327; the exact-one-question-mark count adds nothing but shape friction.
- **Verify:** Confirmed at check_trial.py:318-320 in validate_missing. The `question_marks != 1` check is a pure lexical shape proxy for 'the agent asked for the missing input.' Two concrete valid outputs are wrongly rejected: (1) 'Please share the path to the failing log and the exact command you ran.' has 0 '?' → rejected, yet it satisfies the where/how regex (matches 'command') and emits no route token; (2) 'Where does the log live? And what command produced it?' has 2 '?' → rejected, though it is a legitimate compound clarification asking for two genuinely-missing inputs (path + command). The real invariant (ask where/how to fetch; do NOT route/fabricate) is independently and fully enforced by the where/how regex at lines 321-326 and the no-route check at lines 327-328, so the count adds only shape friction and forces worse prose. Severity high: imperative and two-part clarifications are common natural phrasings, yielding frequent false-negatives against correct agent behavior.

### `check_trial.py:358` — MEDIUM · SHAPE-POLICING
- **Proxy:** Same '?' character count proxy for 'asked for more info' in the no-match/needs-info case.
- **Code:** `question_marks = output.count("?") + output.count("？") ⏎ if question_marks != 1: ⏎     findings.append(f"no-match response must ask one question; found={question_marks}")`
- **Concrete failure:** A valid needs-info reply 'I couldn't classify this; send me the full stack trace and the repro command.' has zero question marks → rejected. A two-part single clarification with two '?' is also rejected.
- **Preserve:** Real intent (needs-info, don't route) is already covered by the frontmatter route=='needs-info' check (line 349), the signals-false checks, and the 'needs-info' output substring (line 355); the count is pure shape.
- **Verify:** Confirmed at check_trial.py:357-359. `question_marks != 1` is a lexical/shape proxy for "agent solicited more info." Real false-neg: a valid needs-info reply phrased imperatively — "I couldn't classify this; send me the full stack trace and the repro command." — has zero `?` and is rejected; a legit two-question clarification (two `?`) is also rejected by `!= 1`. The actual invariant (don't route, ask for input) is already covered structurally by the frontmatter route=='needs-info' + surface/symptom/cause checks (346-349), the five signals:false checks (350-352), no-handoff check (353-354), the 'needs-info' output substring (355-356), and single-evidence-SSoT (336-338). The `?` count guards none of the protected invariants, so removing/replacing it with a semantic solicitation check loses no correctness. Corrected to medium because it is confined to the eval gate and is one of several checks (unlike missing-data, no-match has no semantic solicitation backup, but the count is still shape-policing, not an invariant).

### `check_trial.py:285` — MEDIUM · SHAPE-POLICING
- **Proxy:** A single-line, order-dependent regex stands in for the semantic fact 'three prior fixes failed', forcing the author to phrase it one specific way.
- **Code:** `if count < 3 and not re.search(r"(?:three|3).*(?:fix|attempt)", body, re.IGNORECASE): ⏎     findings.append("architecture route omits three failed fixes")`
- **Concrete failure:** A body that documents the three failures across bullet lines — 'Attempt #1 reverted', 'Attempt #2 reverted', 'Attempt #3 reverted' — is rejected: '.*' does not cross newlines (no DOTALL) and the number must precede 'fix'/'attempt' (here 'attempt' precedes the number). 'Patches A, B and C all regressed' is likewise rejected despite clearly naming three failed fixes.
- **Preserve:** The legitimate structural signal is frontmatter repro_count>=3 (parsed at line 282); that numeric check should stay. Only the prose-fallback regex is the lexical proxy.
- **Verify:** Confirmed at check_trial.py:285. The check is `count < 3 and not re.search(r"(?:three|3).*(?:fix|attempt)", body, IGNORECASE)`, where the numeric frontmatter path (repro_count>=3, line 282) is the primary signal and the regex is a lenient prose fallback. The regex is a genuine lexical/shape proxy for the semantic fact "three prior fixes failed": it is order-dependent (a "3"/"three" token must precede "fix"/"attempt") and line-bounded (no re.DOTALL, so `.*` cannot cross newlines). Verified both concrete false-negatives: "Attempt #1 reverted\nAttempt #2 reverted\nAttempt #3 reverted" fails because the only "3" (in "#3 reverted") is followed by "reverted" not fix/attempt on the same line and matching cannot cross the newline; "Patches A, B and C all regressed" fails because it has no "3"/"three" token at all — both clearly document three failed fixes yet are rejected when repro_count<3. So it wrongly rejects semantically valid input by policing prose shape.

### `check_trial.py:198` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A verb-adjacency regex is treated as proof the agent claimed it auto-invoked the next skill, but it fires on explicit negations too.
- **Code:** `if re.search(rf"\b(?:invoked|started|ran)\s+{re.escape(expected_agent)}\b", output, re.IGNORECASE): ⏎     findings.append("user-visible result claims downstream invocation")`
- **Concrete failure:** Output 'I have not invoked wayne-test-design; you must run it manually.' contains 'invoked wayne-test-design' → flagged as claiming downstream invocation — even though it explicitly disclaims it. This collides with lines 194/196, which REQUIRE the output to name the next agent and mention manual triggering.
- **Preserve:** The real invariant (agent must not auto-advance the pipeline) is genuine, but the file already proves non-invocation deterministically via checkpoint Auto-advance NO (line 192) and no-checkpoint checks; this negation-blind regex should not gate on prose.
- **Verify:** Confirmed at line 198: regex `\b(?:invoked|started|ran)\s+{expected_agent}\b` (IGNORECASE) is negation-blind. Input "I have not invoked wayne-test-design; you must run it manually." contains substring "invoked wayne-test-design" → matches → wrongly flagged as "claims downstream invocation", even though it explicitly disclaims it. This is a true LEXICAL-FALSE-POS: it collides with lines 194/196, which REQUIRE naming the next agent and mentioning manual triggering, so a correct careful output can satisfy those and still trip 198. The check is a lexical proxy for a semantic judgment (did the agent claim it auto-invoked?). Note the trigger band is narrower than the summary suggests — only past-tense verbs invoked/started/ran adjacent to the slug fire; "run it manually" ("run") does not match "ran" — so the false positive requires negated-past-tense phrasing, hence medium not high.

### `check_trial.py:278` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** The bare substring 'bug' stands in for 'output stated the category is bug', so any word containing 'bug' satisfies it.
- **Code:** `if not re.search(r"(?:category\s*[=:]\s*)?bug", output, re.IGNORECASE): ⏎     findings.append("multiple-signal tracker output omits bug category")`
- **Concrete failure:** Output 'I attached the debugger and traced the tokenizer off-by-one; ready-for-agent.' matches 'bug' inside 'debugger' → passes the bug-category gate without the agent ever surfacing a category classification.
- **Preserve:** The real category is validated by frontmatter symptom_class membership (line 271); this output check merely wants the classification surfaced to the user and should match a category token, not any 'bug' substring.
- **Verify:** Verified at check_trial.py:278. The regex `(?:category\s*[=:]\s*)?bug` makes the `category[=:]` prefix optional, so it degenerates to a bare case-insensitive substring `bug`. re.search matches "bug" inside "debugger" (and "debug"/"debugging"), so the concrete failure input 'I attached the debugger and traced the tokenizer off-by-one; ready-for-agent.' passes the bug-category gate without any category ever being surfaced. Since "debug" is ubiquitous in triage output, this gate is effectively a no-op — a lexical proxy standing in for the semantic judgment 'did the output surface the category classification'. The real invariant (category membership) is enforced independently at line 271 on frontmatter.symptom_class (bug/crash/config-env), a legitimate schema/membership check; line 278 is a distinct output-surfacing check and tightening it (anchor to a category token) does not touch line 271. Extra corroboration: 271 allows crash/config-env but 278 hardcodes only 'bug', so a legitimate crash-classified output would be wrongly flagged (false-neg) — confirming the shape-policing lexical nature.

### `check_trial.py:327` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Presence of any route name is treated as 'agent routed', flagging outputs that mention a route only to explain they are NOT routing.
- **Code:** `if re.search(r"fix-now|needs-plan|route-to-owner|ready-for-agent", output, re.IGNORECASE): ⏎     findings.append("missing-data response routed without input")`
- **Concrete failure:** Output 'I can't assign a route such as fix-now without the failing log — where is it?' names 'fix-now' to justify refusing to route, yet is flagged as 'routed without input'.
- **Preserve:** The real invariant (do not route when data is missing) is legit, but 'the agent chose a route' is a semantic act the substring cannot distinguish from 'the agent named a route to decline it'.
- **Verify:** Confirmed at check_trial.py:327 in validate_missing(). The check is a plain case-insensitive substring match (fix-now|needs-plan|route-to-owner|ready-for-agent) on the agent's prose. A semantically valid missing-data response that NAMES a route only to decline it — e.g. "I can't assign a route such as fix-now without the failing log — where is it?" — passes the one-question and where/how checks but is falsely flagged 'routed without input'. The substring cannot tell 'chose a route' from 'named a route to refuse routing', which is genuine LEXICAL-FALSE-POS shape-policing that punishes clear prose. Crucially, the REAL invariant (do not actually route when data is missing) is already structurally guarded by the file-mutation/state checks at lines 314 (evidence_files -> 'wrote evidence before data existed') and 316 (checkpoints -> 'emitted a handoff'); an actual routing act produces those artifacts. So line 327 is a redundant lexical proxy, not the sole guard, and replacing/removing it does not lose routing detection.

### `check_trial.py:321` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Broad bare-substring alternation stands in for 'asked where/how to obtain the data', matching unrelated words that contain the fragments.
- **Code:** `if not re.search( ⏎     r"where|how|fetch|source|command|content|body|哪里|在哪|如何|怎么|来源|拉取|获取|命令|内容|正文", ⏎     output, ⏎     re.IGNORECASE, ⏎ ): ⏎     findings.append("missing-data response does not ask where/how to fetch")`
- **Concrete failure:** Output 'However, I cannot proceed and need the somebody who owns this.' passes because 'how' is inside 'However' and 'body' is inside 'somebody' — despite never asking where/how to fetch anything. The gate is effectively a no-op for most English text.
- **Preserve:** The intent (the reply actually asks how to obtain the missing input) is real, but only a contextual reader can judge it; the substring set neither proves nor disproves it.
- **Verify:** n/a

### `check_trial.py:261` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requiring the words fail/失败/error in the body stands in for 'the evidence records a failing repro', rejecting repros described by their concrete assertion.
- **Code:** `if "tests.test_tokenizer" not in body or not re.search(r"fail|失败|error", body, re.IGNORECASE): ⏎     findings.append("fix route lacks the supplied failing repro")`
- **Concrete failure:** A body with a Repro section 'assert tokenize("a b") == ["a","b"]  # got ["a b"]' referencing tests.test_tokenizer records the exact failing behavior but contains none of fail/失败/error → rejected as 'lacks the supplied failing repro'.
- **Preserve:** Requiring the fixture module reference 'tests.test_tokenizer' is a reasonable grounding check tied to the fixture and should stay; only the fail/error keyword requirement is the lexical proxy.
- **Verify:** Confirmed at check_trial.py:261. The `re.search(r"fail|失败|error", body)` clause is a lexical proxy for the semantic invariant "the evidence records a failing repro." The supplied fixture repro (cases/failure/case.md:8: `uv run --no-project python -m unittest tests.test_tokenizer`) contains none of these words on its own; the control only passes because calibrate.py:51 appends "— FAIL observed". A valid triage that records the failure via a concrete assertion diff (e.g. `assert tokenize("a b") == ["a","b"]  # got ["a b"]` referencing tests.test_tokenizer) captures the exact failing behavior yet has no fail/error/失败 token, and is wrongly rejected as "lacks the supplied failing repro" — a genuine LEXICAL-FALSE-NEG. The finding correctly isolates only the keyword half and keeps the `tests.test_tokenizer` fixture-grounding half. Severity corrected to low: `error` matches *Error/AssertionError/KeyError and `fail` matches failure/failed/failing, so real stack-trace/test output nearly always hits; the false-neg surface is limited to terse assertion-diff phrasings.

### `check_trial.py:264` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requires the literal word 'enhancement' in prose as a proxy for 'classified as a feature/enhancement request'.
- **Code:** `if not re.search(r"enhancement", output, re.IGNORECASE): ⏎     findings.append("tracker output omits enhancement category")`
- **Concrete failure:** Tracker output 'This is a feature request / improvement, marked ready-for-agent.' correctly conveys the enhancement classification using 'feature request'/'improvement' but lacks the token 'enhancement' → rejected.
- **Preserve:** The category enum is already validated in frontmatter via expected_fields symptom_class=='enhancement' (line 252-255); the output word requirement is a synonym-blind shape check.
- **Verify:** Confirmed at line 264: re.search(r"enhancement", output, IGNORECASE) polices the user-visible output for one literal lexeme as a proxy for the semantic 'classified as enhancement/feature request'. Concrete false-neg holds: an output like 'This is a feature request / improvement, marked ready-for-agent.' conveys the correct classification via synonyms but lacks the token 'enhancement' and is wrongly rejected. The actual category enum is already enforced deterministically on frontmatter (symptom_class=='enhancement', lines 220 + 251-255), so the output-word requirement is a synonym-blind shape check. Genuine LEXICAL-FALSE-NEG. Low severity is correct because 'enhancement' is the natural word matching the enum, so real-world misfire frequency is modest.

## eval-work (7)

### `check_trial.py:281` — HIGH · LEXICAL-FALSE-POS
- **Proxy:** The Chinese branch '并行.*(?:成功|可用)' treats a substring match as proof the agent lied about parallel success, but '可用' is contained in the negation '不可用'.
- **Code:** `if re.search(r"parallel delegation available\s*:\s*yes|并行.*(?:成功|可用)", output, re.IGNORECASE): ⏎     findings.append("Codex falsely claims parallel success after dispatch failure")`
- **Concrete failure:** An honest fallback report on one line, e.g. output '并行分发不可用，已串行回退执行。' ("parallel dispatch UNavailable, fell back to serial"), matches '并行' + '.*' + '可用' (the '可用' inside '不可用') and is wrongly flagged as 'Codex falsely claims parallel success after dispatch failure' — the exact opposite of what the text says.
- **Preserve:** Yes — the anti-gaming intent (an agent must not claim parallel success after a spawn failure) is legit and worth keeping; only the broken '并行.*(?:成功|可用)' proxy misfires. The English 'parallel delegation available: yes' literal is fine and should stay.
- **Verify:** Confirmed at check_trial.py:281. The regex branch `并行.*(?:成功|可用)` fires on `并行分发不可用` because `可用` is a substring of `不可用` and `.*` bridges the intervening chars. The honest fallback report `并行分发不可用，已串行回退执行。` (parallel UNavailable, fell back to serial) is wrongly flagged as 'Codex falsely claims parallel success after dispatch failure' — the exact inversion of its meaning. This is worse than a random false-pos: line 273 (same function, same run gated on 'collab spawn failed') REQUIRES the output to report serial fallback, and the natural Chinese for that is `并行不可用`, so the honest path that satisfies 273 is systematically punished by 281. Genuine LEXICAL-FALSE-POS.

### `check_trial.py:214` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** A fixed set of substrings stands in for the semantic judgment 'does this worker prompt forbid committing', so clear prohibitions phrased differently are rejected.
- **Code:** `if not re.search( ⏎     r"do not commit|no commit|must not commit|forbidden:[^\n]*(?:commit|committing)|不要提交", ⏎     prompt, ⏎     re.IGNORECASE, ⏎ ): ⏎     findings.append(f"{unit} worker prompt omits commit prohibition")`
- **Concrete failure:** A worker prompt that unambiguously forbids commits — e.g. 'Do NOT create any git commits; the orchestrator stages and commits.' or 'Leave the tree uncommitted — committing is the orchestrator's job.' — contains none of 'do not commit' / 'no commit' / 'must not commit' / '不要提交' (words intervene: 'do not create any git commits'), so the trial is failed with 'worker prompt omits commit prohibition'.
- **Preserve:** Partly — the invariant 'the worker must not commit' is real, but its OUTCOME is already enforced deterministically by check_git (commit_count==1, branch==main, no staged changes). This prose check is a redundant lexical proxy; the git check must stay.
- **Verify:** Confirmed at check_trial.py:214-219. The regex alternation (do not commit|no commit|must not commit|forbidden:...(commit|committing)|不要提交) is a lexical proxy for the semantic judgment 'does this prompt forbid committing'. A clear prohibition like 'Do NOT create any git commits; the orchestrator stages and commits.' matches none of the alternatives (words intervene between 'not' and 'commit'), so a semantically valid worker prompt is wrongly failed — a genuine LEXICAL-FALSE-NEG / shape-policing. The underlying commit invariant is independently and deterministically enforced by check_git (lines 83-98: branch==main, commit_count==1, no staged changes), so the lexical prose check is a redundant proxy and can be loosened without losing correctness. Corrected severity to medium: the eval harness dispatches templated known units so the false-neg surface is narrow, and the real outcome is double-guarded.

### `check_trial.py:273` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** A keyword list stands in for 'did the agent report that it ran the units sequentially instead of in parallel'.
- **Code:** `if not re.search(r"serial|串行|fallback|fall back|回退|降级", output, re.IGNORECASE): ⏎     findings.append("Codex subagent-unavailable path did not report serial fallback")`
- **Concrete failure:** Output 'Native dispatch was unavailable, so I ran the two units one after another instead.' correctly reports the fallback but contains none of serial/串行/fallback/fall back/回退/降级, so it is wrongly flagged as hiding the fallback.
- **Preserve:** Yes — honest fallback reporting is a real invariant, and the trace-side check at line 268 (looks for the literal 'collab spawn failed' event) already proves the failure happened. The output-prose keyword check is the lexical proxy part.
- **Verify:** Line 273 is a prose keyword match, not a protected structural guard (not hashing/E-row/decision-coverage/schema/state-closure/file-mutation/snapshot/event-order). The real proof the failure occurred is the trace-side event at line 268 (`collab spawn failed`), and lines 275-280 independently prevent the output from hiding the failure; 281-282 catch false parallel-success claims. Loosening the 273 keyword proxy loses no structural correctness.

### `check_trial.py:275` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** A closed set of literal error strings stands in for 'did the user-visible output disclose the dispatch failure'.
- **Code:** `if not ( ⏎     "collab spawn failed" in output.lower() ⏎     or "no thread with id" in output.lower() ⏎     or re.search(r"subagent.*(?:unavailable|failed)|子代理.*(?:不可用|失败)", output, re.IGNORECASE) ⏎ ): ⏎     findings.append(f"Codex output hides dispatch failure: {reason}")`
- **Concrete failure:** Output 'Codex could not spawn parallel workers, so I fell back to serial.' discloses the failure plainly but matches none of 'collab spawn failed' / 'no thread with id' / 'subagent (unavailable|failed)' ('could not spawn workers' != 'subagent ... failed'), so it is flagged as 'Codex output hides dispatch failure'.
- **Preserve:** Yes — disclosing the failure is a real transparency invariant, and the trace-side literal check (line 268) already confirms the failure occurred; this is the redundant lexical-proxy half.
- **Verify:** Confirmed at check_trial.py:275-280. The check uses a closed set of literal error strings ("collab spawn failed", "no thread with id", "subagent...(unavailable|failed)") as a proxy for the semantic judgment "did the output disclose the dispatch failure." The concrete input "Codex could not spawn parallel workers, so I fell back to serial." discloses the failure plainly: line 273-274 passes because "serial" is present, but line 275-280 fires "Codex output hides dispatch failure" because none of the sanctioned tokens match ("could not spawn parallel workers" contains no "subagent"). This is a real LEXICAL-FALSE-NEG — a legitimate, clearly-worded disclosure is rejected for not using template vocabulary. The objective proof that the failure occurred is the trace-side check at line 268 (reads the real trace); the 275-280 output check is the redundant lexical-proxy half. guts_real_invariant=false: loosening it to accept semantic disclosures touches no protected structural guard (the line-268 trace check, which is the actual invariant, is untouched). Severity medium: causes false eval failures on natural-language disclosures, friction not silent correctness loss.

### `check_trial.py:302` — MEDIUM · SHAPE-POLICING
- **Proxy:** A phrase list stands in for 'does the handoff state a scope boundary', forcing a canned phrase instead of a clear statement.
- **Code:** `if not re.search( ⏎     r"out[- ]of[- ]scope|scope is limited|scope[_ -]preserved|preserved[_ -]scope|preserved scope|do not alter", ⏎     text, ⏎     re.IGNORECASE, ⏎ ): ⏎     findings.append("handoff omits an explicit scope boundary")`
- **Concrete failure:** A handoff whose Scope section reads 'Changes are confined to src/relay/models.py and src/relay/service.py only; nothing else was touched.' clearly bounds scope but matches none of out-of-scope / scope is limited / scope preserved / do not alter, so it is failed with 'handoff omits an explicit scope boundary'.
- **Preserve:** Weakly — the required-artifact needles at line 291 (PLAN, MATRIX, I1, I2, verify command) already give the handoff structural coverage; the actual repo-scope invariant is enforced by the manifest/ALLOWED_* diff checks, not by this prose regex.
- **Verify:** Confirmed at check_trial.py:302-307. The regex is a fixed phrase whitelist (out-of-scope / scope is limited / scope preserved / preserved scope / do not alter) standing in for the semantic judgment "does this handoff bound its scope." The concrete input 'Changes are confined to src/relay/models.py and src/relay/service.py only; nothing else was touched.' clearly and unambiguously bounds scope yet matches no alternative, so it is wrongly failed with 'handoff omits an explicit scope boundary'. This is genuine SHAPE-POLICING / LEXICAL-FALSE-NEG forcing a canned phrase over clear prose. Crucially, the actual repo-scope invariant is enforced elsewhere: line 317 (changed - ALLOWED_NORMAL diff/file-mutation check) and lines 320-322 (required_source subset). This regex only inspects prose in handoff.md, so it protects no correctness invariant that the diff checks don't already cover.

### `check_trial.py:296` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** A four-phrase list stands in for the judgment 'does this handoff identify that the work/implementation stage is complete'.
- **Code:** `if not re.search( ⏎     r"status\s*:\s*built|pipeline stage.*work|work handoff|implementation handoff", ⏎     text, ⏎     re.IGNORECASE, ⏎ ): ⏎     findings.append("handoff does not identify completed work stage")`
- **Concrete failure:** A handoff header 'Stage: implementation complete — ready for review' conveys exactly the completed work stage but matches none of 'status: built' / 'pipeline stage ... work' / 'work handoff' / 'implementation handoff' (the words 'implementation' and 'handoff' are not adjacent, and 'pipeline stage.*work' needs the literal word 'work'), so it is failed.
- **Preserve:** No independent structural guard for this exact fact, but the concept is soft; the specific-artifact needles at line 291 already establish this is a work-stage handoff. Only the prose-pattern requirement is a lexical proxy.
- **Verify:** The line 296 check reduces a soft semantic judgment ("does this handoff identify the completed work stage") to a four-phrase whitelist. The concrete input "Stage: implementation complete — ready for review" clearly conveys the completed work stage yet matches none of the alternatives: no "status: built", no literal "pipeline stage" + "work", "work handoff" absent, and "implementation"/"handoff" not adjacent for "implementation handoff". So a semantically valid handoff is rejected — a genuine LEXICAL-FALSE-NEG / shape-policing proxy. This sits on top of the already-structural anchors at line 291 (PLAN, MATRIX, I1, I2, verify.py) and line 294 (wayne-code-review routing), which independently establish it is a work-stage handoff, so the prose-pattern line adds brittleness without adding real coverage. Minor caveat: a full handoff mentioning "wayne-work handoff" would incidentally match "work handoff", so impact is bounded, and this is an eval harness where output format is somewhat controlled — hence low severity, not higher.

### `check_trial.py:220` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Presence of the word 'matrix' stands in for 'the prompt told the worker the orchestrator owns the status-tracking file and the worker must not touch it'.
- **Code:** `if not re.search(r"matri(?:x|ces)|test matrix|矩阵", prompt, re.IGNORECASE): ⏎     findings.append(f"{unit} worker prompt omits main-owned matrix boundary")`
- **Concrete failure:** A prompt 'Do not edit docs/test-matrix/2026-07-17-parallel-units-matrix.md — the orchestrator owns that status file.' communicates the boundary via the exact path but, because it never uses the bare token 'matrix'/'matrices'/'矩阵' (the path substring 'test-matrix' contains 'matrix' — actually this one would match; but) a prompt referring to it as 'the status-tracking .md file owned by main' states the boundary yet matches nothing and is flagged. Conversely, any prompt merely containing the word 'matrix' passes even if it never states the boundary (false-pos direction).
- **Preserve:** Yes — the matrix-untouched invariant is real, but its OUTCOME is enforced deterministically by check_parallel_matrix (line 189, ‘☑→☐’ freeze compare). This prompt-word check is the lexical proxy layered on top.
- **Verify:** Confirmed against code.

## eval-goal-prompt (12)

### `check_trial.py:110` — MEDIUM · SHAPE-POLICING
- **Proxy:** An exact count of question marks stands in for the judgment 'asked one pointed clarifying question'.
- **Code:** `if len(re.findall(r"[?？]", output)) != 1: ⏎     findings.append("vague input does not ask exactly one pointed question")`
- **Concrete failure:** Output ' 你指的是邮件重试还是支付重试？能具体说下是哪一个吗？' is a single focused clarification but has two '？' → count != 1 → wrongly rejected. Conversely an imperative clarification with zero marks ('请告诉我你要重试的是邮件还是支付。') resolves the ambiguity but has 0 marks → also rejected.
- **Preserve:** Loosely guards 'ask one question, don't dump many'. Preserve the intent (single clarification, no invented goal) but stop enforcing it via an exact '?' count; the invented-goal guard on line 106 already covers over-answering.
- **Verify:** Confirmed at check_trial.py:110. `len(re.findall(r"[?？]", output)) != 1` is a pure lexical count standing in for the judgment "asked one pointed clarifying question." Concrete false-negs hold: (1) a single focused clarification with a softening follow-up — "你指的是邮件重试还是支付重试？能具体说下是哪一个吗？" — has two ？ and is wrongly rejected; (2) an imperative clarification "请告诉我你要重试的是邮件还是支付。" with zero marks is also rejected though it resolves the ambiguity. Both are natural Chinese outputs. The real semantics are already covered by line 106 (invented-goal / over-answering) and line 112 (does the question resolve the email/payment ambiguity), so line 110 adds only shape policing that forces exactly one "?" character. This is genuine SHAPE-POLICING.

### `check_trial.py:87` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** The substring 'run the tests' is treated as proof of vague verification regardless of whether a concrete command follows.
- **Code:** `if re.search(r"(?i)works well|looks good|run (?:the )?tests\b", block): ⏎     findings.append("goal uses vague verification or completion language")`
- **Concrete failure:** A precise verification line 'Run the tests: `uv run --no-project python -m unittest discover -s tests -v`' contains 'Run the tests' → flagged as vague, even though check_compose separately REQUIRES that exact command literal. The natural English intro to the required command trips the vagueness flag.
- **Preserve:** It does try to ban genuinely vague sign-off phrases. Keep flagging bare 'works well'/'looks good' as a verification result, but 'run (the) tests' should not fire when a concrete command/expected-output token is present.
- **Verify:** Verified at check_trial.py:87. The regex clause `run (?:the )?tests\b` fires on the phrase "run the tests"/"run tests" regardless of whether a concrete command follows. A precise, compliant verification line like `Run the tests: \`uv run --no-project python -m unittest discover -s tests -v\`` both satisfies the required-literal check in check_compose (line 122) / check_plan (line 145) AND is flagged as "vague verification or completion language" by line 87 — a genuine LEXICAL-FALSE-POS / shape-policing defect that penalizes the clearest way to introduce the exact command the checker demands. The required command literal itself does not trip the regex (verified: "run --no-project" and "discover -s tests" both break the `run (?:the )?tests` sequence), so the false positive comes purely from the agent's natural prose intro. Line 87 is a pure lexical vagueness heuristic, not one of the protected invariant categories; the "works well"/"looks good" alternatives are the legit part and should stay, only the "run (the) tests" clause is the proxy. Downgraded severity from high to medium: the trip requires the specific bigram, and the more common precise phrasing "Run `<command>`" does not fire, so a well-composed goal often escapes it — plausible/avoidable rather than reliable breakage.

### `check_trial.py:132` — MEDIUM · SHAPE-POLICING
- **Proxy:** A one-line 'do not ... direct helper/call/replace' phrasing stands in for the semantic 'exercise the real entrypoint, not a fake'.
- **Code:** `if not re.search(r"(?i)do not[^\n]{0,100}(?:direct helper|direct .*call|replace)", block): ⏎     findings.append("goal omits the real-path fake-substitute boundary")`
- **Concrete failure:** Goal states the boundary as 'Must exercise the real retry entrypoint; never stub or mock it.' — semantically identical but has no 'do not ... direct/replace' → rejected. Also `[^\n]{0,100}` forces it onto one line, so a boundary split across two sentences/lines is rejected.
- **Preserve:** The real-path/fake-substitute boundary is a genuine requirement; keep requiring that the goal state it, but recognize equivalent negatives (never stub/mock/fake/bypass the real entrypoint), not just this one template phrasing.
- **Verify:** Confirmed: check_trial.py:132 requires the regex `(?i)do not[^\n]{0,100}(?:direct helper|direct .*call|replace)` to match the goal `block`, else it appends "goal omits the real-path fake-substitute boundary". This is a genuine SHAPE-POLICING lexical proxy for a semantic requirement (the goal must state a real-entrypoint vs. fake-substitute boundary). Concrete false-negative holds: a semantically identical goal such as "Must exercise the real retry entrypoint; never stub or mock it." contains no "do not" and none of the alternation tokens (direct helper / direct...call / replace), so it is wrongly rejected. Even keeping the "do not" opener fails on valid phrasings: "Do not stub or mock the retry entrypoint" — stub/mock are not in the alternation. The `[^\n]{0,100}` window also forces the boundary onto a single line, rejecting a boundary split across two sentences/lines. The check policies one template phrasing rather than the meaning (real path, not a fake). This is not one of the protected deterministic checks — nearby literal-membership checks (src/retry.py, RETRY_API_TOKEN, etc.) and the fixture-secret leak check are legit and untouched by this finding.

### `check_trial.py:134` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Requires the exact words 'non-transient' or 'other exception' as proxy for 'documents the negative/error-propagation behavior'.
- **Code:** `if not re.search(r"(?i)non[- ]transient|other exception", semantic_block): ⏎     findings.append("goal omits non-transient negative behavior")`
- **Concrete failure:** Goal says 'errors that are not retryable must propagate unchanged; only TransientError triggers a retry' — fully specifies the negative behavior but contains neither 'non-transient' nor 'other exception' → wrongly rejected.
- **Preserve:** Documenting negative behavior is a real requirement; keep it but accept equivalent expressions (permanent/non-retryable/other errors propagate/re-raise) rather than two fixed phrases.
- **Verify:** Confirmed at line 134: `re.search(r"(?i)non[- ]transient|other exception", semantic_block)` accepts only two frozen surface phrasings as a proxy for the semantic property named in the finding string ("negative behavior"). The concrete false-neg holds: "errors that are not retryable must propagate unchanged; only TransientError triggers a retry" fully documents the negative/error-propagation behavior but matches neither phrase (TransientError does not satisfy the `non`-prefixed `non[- ]transient`), so a valid goal is wrongly flagged. Equivalent forms (permanent/non-retryable/other errors re-raise/propagate) all fail identically. This is a LEXICAL-FALSE-NEG / SHAPE-POLICING check, distinct from the legit verbatim-literal presence checks (src/retry.py, TransientError, exact test command, RETRY_DEMO_OK) and the secret-leak guard in the same function, which must stay.

### `check_candidate_static.py:19` — MEDIUM · SHAPE-POLICING
- **Proxy:** Each documentation requirement is verified by requiring a fixed prose phrasing in SKILL.md, standing in for 'the body actually explains this concept'.
- **Code:** `BODY_PATTERNS = { ⏎     "six-section contract": r"Verification required before completion.*Completion criteria", ⏎     "length ceiling": r"4,000 characters", ⏎     "one question": r"ask exactly one Chinese question", ⏎     "plan SSoT": r"plan already owns the work.*SSoT", ... }`
- **Concrete failure:** A SKILL.md that documents the ceiling as 'keep the goal under 4000 characters' (no comma) fails the r'4,000 characters' pattern; one that says 'ask a single Chinese clarifying question' fails r'ask exactly one Chinese question'; one that says 'the plan is the source of truth for the work' fails r'plan already owns the work.*SSoT'. All are correct docs rejected for wording.
- **Preserve:** It guards the real 'SKILL.md must document these contract points'. Keep requiring the concepts, but these are prose-wording proxies for a semantic completeness judgment a regex cannot make; alternate valid phrasings are wrongly failed.
- **Verify:** n/a

### `check_candidate_static.py:76` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A heading literally named 'When to Run' is treated as copied global boilerplate purely by its name.
- **Code:** `if re.search(r"(?im)^## (?:Inherits|When to Run)\b", body): ⏎     findings.append("body contains copied global/routing section")`
- **Concrete failure:** A candidate with a skill-specific '## When to Run' section describing this skill's own trigger conditions (not the copied global routing table) is flagged as containing copied global content, forcing the author to rename a perfectly appropriate heading.
- **Preserve:** It guards 'don't copy the global Inherits/routing boilerplate into the skill'. Keep that intent but detect the copied content, not the heading string — a legitimately named section is not evidence of copying.
- **Verify:** Confirmed at line 76: `re.search(r"(?im)^## (?:Inherits|When to Run)\b", body)` unconditionally emits "body contains copied global/routing section" whenever a heading is literally named "## When to Run" or "## Inherits". This is a real LEXICAL-FALSE-POS: "When to Run" is a natural, appropriate heading for a skill describing its OWN trigger conditions in original prose, yet it is flagged as "copied global content" based solely on the heading string — the check never inspects whether the section content is actually the copied global routing/inherits boilerplate. It makes a semantic ("copied") claim from a heading-name regex proxy. Concrete failure verified: a candidate SKILL.md whose body has `## When to Run` with skill-specific trigger prose (not the global routing table) is rejected, forcing the author to rename a perfectly good heading. The legitimate intent (don't paste global boilerplate) survives if you detect the copied content instead of the heading name.

### `check_trial.py:112` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** A closed vocabulary of trigger words stands in for 'the question actually disambiguates the retry target'.
- **Code:** `if not re.search(r"email|payment|邮件|支付|哪个|哪一", output, re.IGNORECASE): ⏎     findings.append("question does not resolve the ambiguous retry target")`
- **Concrete failure:** Clarifying question '你要重试的是收信流程还是扣款流程？' correctly disambiguates the two candidate targets using 收信/扣款 (synonyms for email/payment) but matches none of email/payment/邮件/支付/哪个/哪一 → wrongly rejected.
- **Preserve:** It weakly guards 'question targets the ambiguity, not something random'. This is genuinely a semantic judgment (whether the question resolves the fixture's ambiguity) that a substring whitelist cannot make reliably.
- **Verify:** Confirmed at check_trial.py:112. The fixture vague-missing/repo/src contains exactly two candidate targets (payment_retry.py, email_retry.py), so the check must verify the clarifying question disambiguates payment vs email. It does so with a bare substring whitelist `email|payment|邮件|支付|哪个|哪一`. This is a lexical proxy for a semantic judgment: (1) false-positive escape valve — a generic '你想重试哪个？' matches `哪个` yet names no target and disambiguates nothing; (2) genuine false-negative — a correct, natural question '你要重试的是收信流程还是扣款流程？' uses the A还是B structure with valid synonyms 收信/扣款 and matches none of the six tokens, so it is wrongly rejected. It cannot actually verify the question resolves the ambiguity; it polices vocabulary. This is a real LEXICAL-FALSE-NEG.

### `check_trial.py:106` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Bare presence of the substring 'Goal:' is treated as 'invented a full goal' even when it is only referenced/quoted.
- **Code:** `if "Goal:" in output or "Verification required" in output: ⏎     findings.append("vague input was converted into an invented goal")`
- **Concrete failure:** A clarifying reply 'I can't fill in the Goal: section yet — which retry target do you mean?' names the template section 'Goal:' to explain why it is stopping, and is wrongly flagged as having invented a goal.
- **Preserve:** It guards the real 'don't fabricate a goal from vague input' invariant. Keep the invariant but detect it by structure (a populated multi-section goal block) rather than any occurrence of the token 'Goal:'.
- **Verify:** n/a

### `check_candidate_static.py:78` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Any occurrence of a protocol token in SKILL.md is treated as 'inlining runtime detail', even a pointer reference.
- **Code:** `for literal in ("thread/start", "thread/goal/set", "turn/start", "thread/inject_items", "RTM_NEWADDR"): ⏎     if literal in body: ⏎         findings.append(f"body inlines runtime protocol detail: {literal}")`
- **Concrete failure:** SKILL.md body containing 'see references/dispatch-runtime.md for the thread/start protocol' mentions 'thread/start' only to redirect the reader, yet is flagged as inlining runtime detail — the exact tokens the same script REQUIRES to be present in the reference file (RUNTIME_LITERALS).
- **Preserve:** It enforces the real architectural boundary 'protocol detail lives in the reference, not SKILL.md' (RTM_NEWADDR here is a legit copy-paste sentinel — keep it). But a bare substring match cannot tell inlining from a citation/pointer.
- **Verify:** Confirmed at check_candidate_static.py:78-80. The check is a bare substring `if literal in body` test with no context awareness. A legit, best-practice pointer sentence such as "See `references/dispatch-runtime.md` for the `thread/start` handshake and `turn/start` protocol" is wrongly flagged as "inlines runtime protocol detail" even though it only cites the reference. This is aggravated by lines 70-71, which REQUIRE the body to contain `references/dispatch-runtime.md`: the most informative way to signpost that reference (naming the handshake it documents) is exactly what the literal check forbids, so the two checks collide and force vaguer prose to pass. Genuine LEXICAL-FALSE-POS / shape-policing. Severity is correctly low because authors can avoid it by writing "the dispatch protocol" without naming literals, but the substring proxy still cannot tell inlining from citation.

### `check_candidate_static.py:81` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Any substring 'gstack' anywhere in any file is treated as a forbidden-dependency reference.
- **Code:** `if "gstack" in "\n".join( ⏎     path.read_text(encoding="utf-8", errors="ignore") ⏎     for path in root.rglob("*") if path.is_file() ⏎ ).lower(): ⏎     findings.append("candidate references forbidden dependency")`
- **Concrete failure:** A doc line that deliberately warns 'do not depend on gstack; use the JSONL monitor instead' contains the substring 'gstack' and is flagged as referencing the forbidden dependency — the check punishes the candidate for documenting the prohibition.
- **Preserve:** It guards a real 'no gstack dependency' rule. Keep the guard but scope it to actual dependency/usage sites (imports, invocations) rather than any lexical occurrence including prohibitions or comments.
- **Verify:** Confirmed at check_candidate_static.py:81-86. The code joins the text of every file under the candidate root, lowercases it, and flags "candidate references forbidden dependency" whenever the substring 'gstack' appears anywhere — with zero scoping to imports, invocations, or dependency declarations. This is a genuine LEXICAL-FALSE-POS: a candidate that documents the prohibition (e.g. "do not depend on gstack; use the JSONL monitor instead") contains the literal 'gstack' and gets flagged for referencing the very thing it forbids. This is especially plausible because the skill contract itself documents negative rules (the JSONL-monitor pattern is literally 'do not scrape a TUI'), so a candidate warning against gstack is natural and valid. Any innocent token containing the substring (logstack, debugstack) would also trip it. The concrete failure holds.

### `check_candidate_static.py:97` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** An exact-spacing JSON literal in the driver source stands in for 'the driver sets status active / injects items'.
- **Code:** `for literal in ("RESUMABLE", "resume.request", '"status": "active"', '"thread/inject_items"'): ⏎     if literal not in driver: ⏎         findings.append(f"goal driver omits {literal}")`
- **Concrete failure:** A driver that builds the request programmatically — e.g. json.dumps({'status': 'active'}) or a dict {"status":"active"} without the space after the colon — is functionally correct but the source never contains the literal '"status": "active"' → wrongly rejected.
- **Preserve:** It guards that the driver implements resume/active-status behavior. That behavior is exercised for real by check_dispatch.py; here the source-substring form fails equivalent implementations that don't match the exact byte sequence.
- **Verify:** Confirmed at check_candidate_static.py:97. The check greps candidate driver SOURCE for exact substrings. The literal '"status": "active"' (colon-space) is a spacing-sensitive lexical proxy for the semantic property "driver activates the goal thread." A functionally-equivalent driver building the request via json.dumps(...) or writing {"status":"active"} (no space) or {'status': 'active'} (single quotes) is wrongly rejected — a genuine LEXICAL-FALSE-NEG. The real driver happens to match only because it hardcodes {"threadId": thread_id, "status": "active"} at codex_goal_driver.py:260. The behavior is independently and truly exercised by check_dispatch.py, so the static substring is a redundant shape proxy. Not gutting a real invariant: it is a source-substring smoke check, not hashing/schema/E-row/state-closure/event-order/snapshot. Severity lowered to low because only this one of the four literals is spacing-fragile; the other three (RESUMABLE, resume.request, thread/inject_items) are distinctive protocol anchors, so realistic blast radius is small.

### `check_candidate_static.py:93` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** The literal case-label 'resume)' stands in for 'the shell handles a resume subcommand'.
- **Code:** `for literal in ("resume)", "WAYNE_DISPATCH_STARTUP_TIMEOUT", "control/ready", "resume.request"): ⏎     if literal not in shell: ⏎         findings.append(f"dispatch shell omits {literal}")`
- **Concrete failure:** A shell script that handles resume via 'resume|r)' or '"resume")' (quoted) or a dispatch/case structured differently is functionally correct but contains no 'resume)' substring → wrongly rejected.
- **Preserve:** It checks the shell implements resume + startup timeout, behavior actually exercised end-to-end by check_dispatch.py. Keep the behavioral coverage; the source-substring proxy here rejects equivalent shell syntaxes.
- **Verify:** Confirmed at line 93. Among the four substrings, three (WAYNE_DISPATCH_STARTUP_TIMEOUT, control/ready, resume.request) are verbatim identifiers/protocol names — legit exact-name checks. But "resume)" is a bash case-label syntax fragment, a lexical proxy for "shell dispatches a resume subcommand." Concrete false-neg holds: idiomatic equivalents `"resume")`, `resume|r)`, or an if-based dispatch all implement resume correctly yet contain no `resume)` substring, so all are wrongly rejected (LEXICAL-FALSE-NEG / SHAPE-POLICING). The finding correctly isolates only this token and preserves the three legit identifier checks. It does not guard any protected invariant (behavioral coverage lives elsewhere; this is a source-substring proxy), so relaxing it improves rather than guts the check.

## eval-code-review (11)

### `check_candidate_static.py:137` — MEDIUM · SHAPE-POLICING
- **Proxy:** Exact English sentence-fragments stand in for 'the playbook teaches this concept', so semantically-equivalent prose is rejected.
- **Code:** `for label, phrase in PLAYBOOK_CONTRACTS.items(): ⏎     if phrase not in normalized_playbooks: ⏎         findings.append(f"review playbooks omit {label}: {phrase}")`
- **Concrete failure:** A playbook that teaches the same rule as 'wrong-value severity' by writing 'If a live consumer reads a corrupted value -> CRITICAL' is FAILed for 'omit wrong-value severity' because it lacks the verbatim phrase 'critical when a real consumer receives a wrong value'. Same for 'planned-missing direction': 'Detect planned-but-absent behavior: the plan specified X, the diff omits it' lacks 'planned behavior missing from the diff'.
- **Preserve:** Yes - the playbook genuinely must cover these review directions/severities; keep the intent (coverage) but the exact-substring match cannot judge coverage. Structural presence-of-each-review-type (REVIEW_TYPES loop) is legit and must stay.
- **Verify:** Confirmed at check_candidate_static.py:136-138. The loop does a plain `phrase not in normalized_playbooks` substring test yet emits "review playbooks omit {label}" — asserting a SEMANTIC absence from a purely LEXICAL absence. Several PLAYBOOK_CONTRACTS values are full English sentence-fragments ("critical when a real consumer receives a wrong value", "planned behavior missing from the diff", "ownership, module boundaries, lifecycle, persistent state", "single owner for each state", "small pure function or local bug fix"). A playbook that teaches the identical rule with different words — e.g. "A wrong value delivered to a live consumer is CRITICAL severity" — contains the concept fully but lacks the verbatim substring and is FAILed. Reordering the comma-list terms or using synonyms also fails. This is a lexical proxy for a completeness/coverage judgment the deterministic code cannot make (MISCLASSIFIED-ORACLE / lexical-false-neg). The concrete failure genuinely holds.

### `check_trial.py:205` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Requires the hyphenated category slug 'shell-command-injection' verbatim in prose, so the natural spaced phrasing is rejected.
- **Code:** `for needle in ("shell-command-injection", "src/archive.py", "critical", "claude", "codex"): ⏎     if needle not in lower: ⏎         findings.append(f"synthesis omits confirmed finding evidence: {needle}")`
- **Concrete failure:** A correct synthesis 'Both voices confirmed the shell command injection in src/archive.py (CRITICAL)' contains 'shell command injection' (spaces) not 'shell-command-injection' (hyphens) and is FAILed with 'synthesis omits confirmed finding evidence: shell-command-injection'. Line 210 has the identical defect for 'overwrite-default-compatibility'.
- **Preserve:** Yes - synthesis must cite the confirmed finding and both provider names; keep that, but the slug should match spaced/hyphen variants, not force one lexical form.
- **Verify:** Confirmed at check_trial.py:205-207. The loop requires the hyphenated slug 'shell-command-injection' as a literal substring of output.lower(); line 210 does the same for 'overwrite-default-compatibility'. This is a lexical shape-policing proxy: a semantically correct synthesis using the natural spaced form ('shell command injection') is FAILed with 'synthesis omits confirmed finding evidence: shell-command-injection'. The smoking gun is in the same file — check_security at line 163 matches the identical concept flexibly via regex (shell\s*(?:command\s*)?injection|command injection|命令注入), proving the authors know the concept has multiple valid forms yet froze one lexical variant here. The concrete false-negative genuinely holds.

### `check_trial.py:171` — MEDIUM · SHAPE-POLICING
- **Proxy:** A specific 'no findings' phrasing stands in for 'the review concluded clean', rejecting other clean conclusions.
- **Code:** `if not re.search(r"no findings|0\s+(?:findings|issues)|未发现.*安全|无.*安全.*问题", output, re.IGNORECASE): ⏎     findings.append("safe security review does not report a clean result")`
- **Concrete failure:** A correct clean review 'No security issues found; the subprocess call uses an argv list, so there is no shell to inject into.' is FAILed because 'no security issues' does not contain the bigram 'no findings' and there is no '0 findings/0 issues' either.
- **Preserve:** Yes - the safe-neighbor case must not silently pass a review that found the (non-existent) vuln; keep the clean-conclusion requirement but broaden the accepted phrasings.
- **Verify:** Line 171 uses a narrow lexical whitelist (`no findings|0 findings/issues` + two Chinese idioms) as a proxy for the semantic judgment "the review concluded clean." It genuinely rejects valid clean conclusions such as "No security issues found", "No vulnerabilities detected", or "The code is safe" — the concrete failure input holds because none contain the "0"-digit form or the exact bigram "no findings". This is SHAPE-POLICING of the free-text output. Mitigating: wayne-code-review's canonical verdict term is literally "NO FINDINGS", which the case-insensitive alternative matches, so the standard happy-path output passes and real-world false-negs are less frequent than the finding's "high" implies — hence medium, not high.

### `check_candidate_static.py:148` — MEDIUM · SHAPE-POLICING
- **Proxy:** A regex for the phrase 'exactly two ... voices' stands in for the invariant 'the skill mandates precisely two voices'.
- **Code:** `if not has_any(body, (r"\bexactly\s+two\b[^.\n]{0,80}\b(?:voices?|reviewers?)\b", r"\b(?:two|2)\s+exact\b...", r"\b(?:voices?|reviewers?)\b[^.\n]{0,80}\bexactly\s+(?:two|2)\b")): ⏎     findings.append("SKILL.md must require exactly two review voices")`
- **Concrete failure:** A SKILL.md that states 'Run one Claude reviewer and one Codex reviewer. Never add a third.' mandates exactly two voices but matches none of the regexes (no 'exactly two' co-occurring with voices/reviewers within 80 chars) and is FAILed.
- **Preserve:** Yes - two-voice mandate is a real contract; but a doc-prose regex cannot verify a normative property. Keep the intent, not the grammar policing.
- **Verify:** Confirmed at lines 148-156. The three regexes all require the literal token "exactly two" (or "two exact") to co-occur within 80 chars of voices/reviewers. A natural, unambiguous mandate like "Run one Claude reviewer and one Codex reviewer. Never add a third." expresses exactly-two but contains no "exactly two"/"two exact" token, so it matches none of the patterns and is FAILed. This is genuine SHAPE-POLICING: a lexical proxy standing in for a normative property (the two-voice contract) that a prose regex cannot actually verify — it merely forces the author to parrot a fixed phrase. Acting on it (loosening the phrase-shape requirement) touches none of the protected structural guards (hashing, E-carry, decision-coverage, schema, state-closure, file-mutation, exact-snapshot, event-order); the adjacent line-146 token check for both "claude" and "codex" is a separate, structural presence check and remains intact.

### `check_candidate_static.py:158` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Bag-of-words ('same'+'frozen'+'hash') proxies for 'both voices review the identical pinned artifact' - both over- and under-matches.
- **Code:** `if not all(token in body_lower for token in ("same", "frozen", "hash")): ⏎     findings.append("SKILL.md must require both voices to use the same frozen hash")`
- **Concrete failure:** False-neg: 'Both reviewers pin the identical commit SHA; neither may re-diff.' expresses the invariant but lacks 'frozen' and 'hash' (SHA != hash) -> FAILed. False-pos: a doc mentioning 'the same repo', 'frozen fixtures' and a 'hash table' in three unrelated sentences passes without stating the invariant.
- **Preserve:** Yes - both voices must use the same frozen input; keep the requirement but the three-word co-presence does not prove it.
- **Verify:** This check does not compute or verify any hash/freeze — it only tests whether three English words appear in the SKILL.md prose. It is not an actual content-hashing / freeze-verification mechanism (the protected category). The underlying requirement "both voices use the same frozen input" must be preserved, but replacing the three-word co-presence proxy with a proximity/semantic check damages no hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, snapshot, or event-order guard.

### `check_candidate_static.py:189` — MEDIUM · SHAPE-POLICING
- **Proxy:** Forces five tokens (incl. hyphenated 'return-only') to co-occur in a single paragraph as a proxy for the handoff rule.
- **Code:** `for paragraph in re.split(r"\n\s*\n", body_lower): ⏎     normalized = re.sub(r"\s+", " ", paragraph) ⏎     required = ("only", "clean", "pass", "return-only", "wayne-verify") ⏎     if all(token in normalized for token in required): ⏎         handoff_ok = True`
- **Concrete failure:** A SKILL.md that correctly states 'On a clean PASS you may hand off to wayne-verify, and only then, as a return only call.' fails: 'return only' (space) != 'return-only' (hyphen), so no paragraph satisfies all five tokens -> 'must allow a return-only wayne-verify handoff only for a clean PASS'. Splitting the rule across two paragraphs also fails.
- **Preserve:** Yes - handoff-only-on-clean-PASS is a real behavior; keep it, but single-paragraph token co-occurrence is grammar policing.
- **Verify:** Confirmed at check_candidate_static.py:189-199. The paragraph loop normalizes only whitespace (re.sub(r"\s+", " ")) which does NOT fold hyphens, so the literal token "return-only" fails to match a valid doc that writes "return only" with a space. This is inconsistent with line 174's own \breview[- ]only\b pattern, which deliberately accepts both hyphen and space. Additionally, requiring all five tokens ("only","clean","pass","return-only","wayne-verify") to co-occur in a SINGLE paragraph rejects a doc that expresses the same rule across two paragraphs. The concrete input "On a clean PASS you may hand off to wayne-verify, and only then, as a return only call." is semantically correct yet rejected. This is genuine SHAPE-POLICING/LEXICAL-FALSE-NEG: it polices prose shape, not meaning. It does NOT protect a protected structural invariant — it is a documentation word-linter, and the actual handoff-only-on-clean-PASS behavior is enforced by wayne-verify at runtime, not by this token co-occurrence. Relaxing to return[- ]only and/or dropping the single-paragraph constraint loses no correctness guarantee.

### `check_candidate_static.py:171` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Two independent bag-of-words hits anywhere in the body proxy for one causal rule ('voice failure => not PASS').
- **Code:** `if not has_any(body, failure_terms) or not has_any(body, non_pass_terms): ⏎     findings.append("SKILL.md must state that either voice failing cannot produce PASS")`
- **Concrete failure:** False-pos: a body that mentions 'timeout' in a config section and 'cannot pass' in an unrelated sentence about test gates satisfies both term-sets without ever tying voice-failure to non-PASS. False-neg: 'If a reviewer errors, the verdict is BLOCKED' uses neither a failure_term (fail/invalid/unavailable/timeout) nor a non_pass_term -> FAILed.
- **Preserve:** Yes - the fail-loud rule (either voice failing blocks PASS) is real; keep it, but disjoint substring hits don't establish the causal link.
- **Verify:** Confirmed at line 171. has_any() searches the whole body with no proximity/ordering link between the two term-sets, so it is a bag-of-words proxy for the causal rule "voice failure => not PASS". False-neg holds: "If a reviewer errors, the verdict is BLOCKED" states the exact rule but matches no failure_term ("errors" != fail/invalid/unavailable/timeout) and no non_pass_term ("BLOCKED"), so it is wrongly rejected — vocabulary policing forcing worse prose. False-pos holds: any real code-review SKILL.md contains "fail" (test failures) and some "not pass" phrase in unrelated sentences, satisfying both disjoint checks without ever establishing the causal link. Both directions verified.

### `check_trial.py:150` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Accepts Chinese '第8行' but omits the English 'line 8' form, so a correct English review of line 8 is rejected.
- **Code:** `if not re.search(r"src/export\.py:(?:8|[1-8]-[8-9])|第\s*8\s*行", output, re.IGNORECASE): ⏎     findings.append("review omits vulnerable line 8")`
- **Concrete failure:** A correct review 'The vulnerability is on line 8 of src/export.py' is FAILed: the regex requires 'src/export.py:8' contiguous or Chinese '第8行' - plain English 'line 8' is not an alternative (contrast check_dataflow line 195 which DOES include 'line\s*7'). A range report 'src/export.py:5-10' also fails since the range branch is single-digit [8-9].
- **Preserve:** Yes - the review must locate the vuln at line 8; keep the location requirement, add the 'line 8' English form for parity with the dataflow check.
- **Verify:** Verified at check_trial.py:150. The regex `src/export\.py:(?:8|[1-8]-[8-9])|第\s*8\s*行` accepts `src/export.py:8`, colon-ranges ending in 8-9, and Chinese `第8行`, but omits the plain English `line 8` form. A correct English review "command injection at src/export.py, line 8" passes the file check (line 148) yet fails line 150, because after `src/export.py` comes `, line 8` not `:8`. This is a true LEXICAL-FALSE-NEG. The asymmetry is confirmed by the sibling dataflow check at line 195, which DOES include `line\s*7` as an alternative — so the omission of `line\s*8` here is an inconsistency/oversight in the same file, not intent. The range concern also holds for covering ranges not starting at 8 (e.g. `5-10`), though ranges starting at 8 still pass via the bare-8 branch. The fix is additive (add `line\s*8` for parity), so it preserves the legit location requirement without gutting any structural invariant.

### `check_trial.py:187` — LOW · SHAPE-POLICING
- **Proxy:** Requires the dotted symbol 'teamconfig.timeout_ms' as a substring, forcing one specific written form of the identifier.
- **Code:** `required = {"src/delivery/retry.py": "stale consumer path", "teamconfig.timeout_ms": "new state owner", ...} ⏎ for needle, label in required.items(): ⏎     if needle not in lower: ⏎         findings.append(f"dataflow review omits {label}: {needle}")`
- **Concrete failure:** A review 'the timeout_ms field on TeamConfig is the new owner' lowercases to 'teamconfig' and 'timeout_ms' present but NOT the contiguous 'teamconfig.timeout_ms', so it is FAILed with 'omits new state owner: teamconfig.timeout_ms'.
- **Preserve:** Yes - the review must name the new state owner; keep the requirement but accept the possessive/spaced form of the symbol reference.
- **Verify:** Confirmed at check_trial.py:186-194. check_dataflow requires the contiguous substring 'teamconfig.timeout_ms' in the lowercased free-text review output. The fixture (overlay/src/delivery/resolve.py) accesses the new state owner as `config.timeout_ms` (instance `config` of type `TeamConfig`), so the most code-accurate review reference `config.timeout_ms` does NOT contain 'teamconfig.timeout_ms' and is falsely FAILed. The finding's example prose ('the timeout_ms field on TeamConfig') also fails. This is genuine SHAPE-POLICING: it enforces one specific spelling of the identifier rather than the semantic requirement (the review must name the new owner). Multiple legit forms (config.timeout_ms, 'timeout_ms field on TeamConfig', TeamConfig's timeout_ms) all fail; only the exact 'TeamConfig.timeout_ms' spelling passes.

### `check_trial.py:153` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requires the English token 'critical' even though the rest of the harness accepts Chinese review output.
- **Code:** `if "critical" not in output.lower(): ⏎     findings.append("command injection is not classified CRITICAL")`
- **Concrete failure:** A fully-Chinese review that classifies the command injection as '严重' (the Chinese severity, matching the harness's Chinese support at lines 150/155/171) is FAILed for not containing 'critical'. Same at line 197 for the dataflow case.
- **Preserve:** Yes - the finding must be CRITICAL severity; CRITICAL is a controlled token, so keep it, but a bilingual harness should accept '严重' as it does elsewhere.
- **Verify:** Confirmed. Lines 152-153 (and the parallel line 197 in check_dataflow) require the English substring "critical" in the free-form synthesis `output`, while the immediately adjacent checks in the same function deliberately accept Chinese: line 150 (第 8 行), line 155 (命令注入), line 156 (执行.*命令), line 171 (未发现.*安全). A fully-Chinese review that classifies the command injection as 严重 therefore FAILs line 152 despite conveying exactly CRITICAL severity — a genuine bilingual LEXICAL-FALSE-NEG: an English substring proxy standing in for the semantic "reached the critical classification." The controlled-token severity enum is NOT what this line guards: the schema enum {CRITICAL, INFORMATIONAL} is enforced separately on the raw provider JSON via has_shell_finding (line 117, row.get('severity') == 'CRITICAL'), which this finding leaves untouched. Adding a 严重 alternative to the prose check preserves the severity-carry semantic fact (checker-notes item 5) while removing the English-only shape policing, and 严重 unambiguously maps to the sole non-INFORMATIONAL value, so no false-pass risk is introduced.

### `check_candidate_static.py:213` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Case-sensitive 'Agent(' pattern flags generic capitalized 'Agent (…)' prose as hard-coding the Agent tool mechanism.
- **Code:** `(re.compile(r"\bAgent\s*(?:\(|tool\b)"), "Agent tool"), ⏎ ... ⏎ if pattern.search(text): ⏎     findings.append(f"normative document hard-codes {label}: {relative}")`
- **Concrete failure:** A normative doc using 'Agent' as a role noun - 'Each reviewing Agent (Claude or Codex) works in an isolated context' - matches 'Agent (' and is flagged 'hard-codes Agent tool', though it never references the Agent dispatch mechanism.
- **Preserve:** Yes - normative docs genuinely should not hard-code the Agent()/subagent_type mechanism; keep that, but require the actual call shape (e.g. Agent-with-subagent_type), not any 'Agent (' prose.
- **Verify:** Confirmed at line 213: `re.compile(r"\bAgent\s*(?:\(|tool\b)")` is case-sensitive and the `\s*` allows whitespace between `Agent` and `(`. Verified by running the regex: "Each reviewing Agent (Claude or Codex) works in an isolated context" matches ('Agent (') and is reported at lines 218-219 as 'normative document hard-codes Agent tool', a genuine LEXICAL-FALSE-POS. The prose uses 'Agent' as a role noun, not the Agent() dispatch mechanism. The real call shape is 'Agent(' with no space (also matches, correctly); the optional \s* is what over-fires on innocent parenthetical prose. Realistic input for a dual-voice review skill's normative docs.

## eval-test-design (9)

### `check_trial.py:197` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Bare substring 'auth' stands in for the judgment 'this row designs an impossible auth dimension', so it fires on any word containing those letters.
- **Code:** `if any(re.search(r"concurrency|persistence|auth|network", row, re.IGNORECASE) for row in u): ⏎     findings.append("simple matrix over-designs impossible dimensions")`
- **Concrete failure:** A pure string-slug function U-SEED row `| U1 | "Author Name" -> "author-name" | slug lowercasing |` contains 'author' (matches 'auth') and is wrongly flagged 'simple matrix over-designs impossible dimensions', even though it designs no authentication dimension.
- **Preserve:** Yes — the intent (a pure function must not over-design concurrency/persistence/auth/network dimensions) is legit; only the unanchored substring match must be tightened (e.g. word boundaries / cell-scoped keywords), not removed.
- **Verify:** Confirmed at check_trial.py:197-198. The regex r"concurrency|persistence|auth|network" uses unanchored substrings; re.search on "Author Name" matches "auth" (verified empirically) so a legit U-SEED slug row like `| U1 | "Author Name" -> "author-name" | slug lowercasing | ☐ |` is wrongly flagged "simple matrix over-designs impossible dimensions" — a semantic judgment the substring cannot make. Genuine LEXICAL-FALSE-POS: "auth" is a substring of author/authorize/authentication/oauth. This is a lexical heuristic, not a protected invariant guard, and can be tightened (word boundaries / cell-scoped) without loss of the real intent, so guts_real_invariant is false. Severity corrected to medium because the false positive is confined to the narrow "simple" pure-function case and requires an input word that happens to contain these substrings (canonical fixtures use "Hello World"/"Ab C"), so real but low-frequency.

### `check_trial.py:155` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A fixed negation-phrase whitelist stands in for the judgment 'this streaming row does not also claim attestation', forcing the author to use one of five exact phrasings.
- **Code:** `if "stream" in row_lower and ("policy_verified" in row or "attestation_state" in row) and not re.search(r"not claim|does not|cannot|isn't|is not", row_lower): ⏎     findings.append("functional and attestation proof axes are coupled")`
- **Concrete failure:** Row `| E3 | user streams answer | ... | tokens stream progressively; policy_verified proven SEPARATELY in E4 | ⬜ |` correctly decouples the two axes but uses 'separately' (not in not claim/does not/cannot/isn't/is not), so it is wrongly flagged 'functional and attestation proof axes are coupled'.
- **Preserve:** Yes — the invariant (a functional stream row must not simultaneously assert native attestation) is real; the lexical negation whitelist is the proxy that must be replaced, not the coupling rule itself.
- **Verify:** Confirmed at check_trial.py:155. The `re.search(r"not claim|does not|cannot|isn't|is not", row_lower)` clause is a fixed 5-phrase negation whitelist used as a lexical proxy for the semantic judgment 'this stream row decouples the functional and attestation axes.' Concrete false-pos holds: a row like `| E3 | user streams answer | ... | tokens stream progressively; policy_verified proven SEPARATELY in E4 | ⬜ |` legitimately decouples via 'separately' (also 'independently', 'in a distinct row', etc.), but none of those are in the whitelist, so the coupling flag wrongly fires. Genuine LEXICAL-FALSE-POS / MISCLASSIFIED-ORACLE. Narrowing note: it only misfires when a stream row also names policy_verified/attestation_state (a cross-referencing pattern), and only affects eval grading of the provider-isolation fixture, so blast radius is limited — hence medium, not high.

### `check_trial.py:178` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A rejection-vocabulary whitelist stands in for the judgment 'this Gamma/encrypted row acknowledges unreachability', flagging rows that express it with other words.
- **Code:** `if "gamma" in row_lower and "encrypted" in row_lower and not re.search( ⏎     r"reject|unavailable|missing|fail|stop|conflict|blocked|no native|absence", ⏎     row_lower, ⏎ ): ⏎     findings.append("matrix authors unreachable positive Gamma capability row")`
- **Concrete failure:** Row `| E5 | Gamma encrypted-reasoning request | ... | runtime exposes nothing to prove it; unverifiable | ⬜ |` correctly marks the capability as unprovable, but 'unverifiable'/'nothing' are absent from the whitelist, so it is wrongly flagged 'matrix authors unreachable positive Gamma capability row'.
- **Preserve:** Yes — the rule (do not author an unreachable positive capability row) is legit; only the fixed word list proxy must be broadened.
- **Verify:** Confirmed at check_trial.py:178. The per-row check flags any Gamma+encrypted row lacking the fixed rejection-vocab regex `reject|unavailable|missing|fail|stop|conflict|blocked|no native|absence`. The most natural words to acknowledge an unprovable capability — "unverifiable", "unprovable", "cannot verify", "nothing to prove" — all escape every alternative (e.g. "unverifiable" != "unavailable", no "fail"/"missing" substring). A correct row such as `| Gamma encrypted-reasoning | --encrypted-reasoning present | requested intent only; runtime exposes nothing to prove it; unverifiable | ⬜ |` correctly marks the capability unreachable yet is wrongly flagged "matrix authors unreachable positive Gamma capability row." This is a genuine LEXICAL-FALSE-POS: a fixed word list proxying for the semantic judgment "does this row acknowledge the Gamma capability is unprovable." The whitelist must be broadened / replaced with a meaning-based check.

### `check_trial.py:62` — MEDIUM · SHAPE-POLICING
- **Proxy:** is_justified_fanout() requires a stack of exact keywords ('alpha','beta', fan-out spelling, 'both', 'fail/down', 'result/outcome/entr') to recognize a semantically valid cross-provider fan-out row.
- **Code:** `return ( ⏎     "alpha" in lower ⏎     and "beta" in lower ⏎     and is_fanout_row(row) ⏎     and ("both" in observable or ("alpha" in observable and "beta" in observable)) ⏎     and re.search(r"fail|down", lower) is not None ⏎     and re.search(r"result|outcome|entr", observable) is not None ⏎ )`
- **Concrete failure:** Row `| E4 | Alpha down -> traffic fails over to Beta; observe both providers return reconciled responses | ⬜ |` is a legit failover/fan-out row, but is_fanout_row() does not match 'fails over' (its regex needs 'fan out'/'fanout'/'fans...out'), so check() reports 'missing justified cross-provider fan-out row' (line 151).
- **Preserve:** Yes — requiring at least one justified fan-out row is a real coverage invariant; the multi-keyword template shape is the proxy that rejects valid phrasings.
- **Verify:** Confirmed real lexical/SHAPE-POLICING proxy. is_justified_fanout() gates the provider-isolation case (line 151) on is_fanout_row(), which only matches literal 'fan-out'/'fanout'/'fans...out'. A semantically valid 7-cell fan-out row using synonymous phrasing (e.g. '| E4 | Query broadcast in parallel to Alpha and Beta | ... | both providers return a reconciled result; if Alpha fails the entry still resolves | ⬜ |') satisfies alpha/beta/both/fail/result but fails is_fanout_row purely on spelling, triggering 'missing justified cross-provider fan-out row'. That is a genuine false-neg policing shape not meaning. Caveat: the finding's OWN concrete example is weak — its row has only 3 cells so it actually fails at len(cells)!=7 (line 66) before is_fanout_row, and 'fails over' describes failover, not fan-out; but a valid counterexample exists, so the defect class holds. Fix is to broaden recognition, not remove the requirement.

### `check_candidate_static.py:59` — MEDIUM · SHAPE-POLICING
- **Proxy:** The required substring embeds a hard newline ('only a\nstandalone'), so the check polices the exact prose line-wrap position rather than the documented meaning.
- **Code:** `"standalone plan handoff route": ( ⏎     'J -> W [label="no"]' in skill ⏎     and "only a\nstandalone, unblocked run hands the artifact to `wayne-plan`" in lower ⏎ ),`
- **Concrete failure:** If the skill reflows the sentence to a single line 'only a standalone, unblocked run hands the artifact to `wayne-plan`' (identical meaning, no mid-phrase newline), the substring is absent and check() reports 'missing intent: standalone plan handoff route'.
- **Preserve:** Yes — verifying the skill documents the standalone->wayne-plan handoff route is legit; the hardcoded '\n' wrap position carries no meaning and must not be part of the match.
- **Verify:** Confirmed at line 59: the required substring "only a\nstandalone, unblocked run hands the artifact to `wayne-plan`" embeds a hard newline mid-phrase, forcing the candidate's prose to wrap between "only a" and "standalone". This is a lexical/shape proxy for a semantic fact. Concrete false-neg holds: reflowing the identical-meaning sentence onto one line ("only a standalone, unblocked run hands the artifact to `wayne-plan`") makes the substring absent, triggering "missing intent: standalone plan handoff route". The reference SKILL.md (line 156) even wraps and words the sentence differently, underscoring the brittleness. The co-located `'J -> W [label="no"]'` DOT-edge check is a legit structural guard and must stay; only the \n-bearing prose match is the defect. Severity corrected high->medium: real false-neg but one of ~14 equally-lexical exact-substring intent checks, trivial one-reword blast radius.

### `check_candidate_static.py:73` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** The approval-before-write invariant is verified by one exact phrase, rejecting synonymous statements of the same gate.
- **Code:** `if not re.search(r"write only after approval", lower): ⏎     findings.append("missing approval-before-write gate")`
- **Concrete failure:** A skill stating 'only write the matrix after the user has approved it' expresses the identical gate but does not contain the contiguous phrase 'write only after approval', so check() reports 'missing approval-before-write gate'.
- **Preserve:** Yes — the approval-before-write gate is a real invariant to require; only the single-phrase lexical proxy is the defect.
- **Verify:** Line 73 `re.search(r"write only after approval", lower)` reduces the semantic "approval-before-write" gate to one exact contiguous phrase. A candidate SKILL.md stating the same gate in synonymous prose (e.g. "only write the matrix after the user has approved it" / "do not write the matrix until the user approves") contains no "write only after approval" substring, so check() wrongly appends "missing approval-before-write gate" — a genuine LEXICAL-FALSE-NEG that policies phrasing, not meaning. The invariant it stands for is real, but the phrase-match itself is not a protected deterministic guard: broadening it damages none of hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, snapshot, or event-order, so guts_real_invariant=false. Severity stays medium (not high) because the whole file is an as-designed exact-phrase intent guard over a single frozen candidate, capping the blast radius to re-authored-prose cases.

### `check_trial.py:190` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requiring the literal token 'typeerror' stands in for 'a wrong-type case exists', rejecting the natural spelling 'type error'.
- **Code:** `for term, present in (("lower", lower_case), ("empty", empty_case), ("typeerror", has_row(u, "typeerror"))):`
- **Concrete failure:** Row `| U3 | passing an int raises a type error | edge |` clearly covers the TypeError behavior but is spelled 'type error' (space), so has_row(u,'typeerror') is False and check() reports 'simple U-SEED misses behavior: typeerror'.
- **Preserve:** Yes — requiring a wrong-type behavior case is legit; only the exact-token match is the proxy.
- **Verify:** Confirmed at line 190: has_row(u, "typeerror") is a literal case-insensitive substring test ("typeerror" in row.lower(), line 46). A U-SEED row spelling the wrong-type behavior as "raises a type error" (with a space) lowercases to "type error", which does not contain "typeerror", so check() emits "simple U-SEED misses behavior: typeerror" — a genuine LEXICAL-FALSE-NEG that policies spelling instead of the semantic requirement "a wrong-type case exists". The smell is reinforced by the two sibling behaviors in the same tuple (lower_case, empty_case) being matched with multiple accepted spellings, while typeerror gets a single rigid token. Mitigations that lower but don't defeat it: the canonical Python spelling "TypeError" DOES pass (lowercases to "typeerror"), and this is an eval-harness golden checker (grades a skill trial), not a validator gating real user prose — so impact is a rare false FAIL rather than everyday friction.

### `check_trial.py:184` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** The presence of a lowercasing case is inferred from the keyword 'lower' (or two hardcoded example pairs), missing synonymous descriptions.
- **Code:** `lower_case = ( ⏎     has_row(u, "lower") ⏎     or has_row(u, '"Hello World"', '"hello-world"') ⏎     or has_row(u, '"Ab C"', '"ab-c"') ⏎ )`
- **Concrete failure:** Row `| U1 | "HELLO" downcased to "hello" | case |` covers lowercasing but says 'downcased', not 'lower', and matches neither literal example, so check() reports 'simple U-SEED misses behavior: lower'.
- **Preserve:** Yes — requiring a case-conversion case is legit; the keyword/example proxy is the defect.
- **Verify:** Confirmed at check_trial.py:184-188 (case "simple"). has_row (line 46) does substring `term.lower() in row.lower()`, so `has_row(u, "lower")` only fires when the literal substring "lower" appears; the two fallbacks require BOTH quoted example literals in one row. A U-row like `| U1 | "HELLO" downcased to "hello" | case |` semantically covers lowercasing but contains neither "lower" nor the example-pair literals, so lower_case=False and line 192 emits `simple U-SEED misses behavior: lower` — a real LEXICAL-FALSE-NEG. Genuine synonyms ("downcased", "casefolded") that omit the spec example literals get wrongly failed. The three OR-branches (keyword + two example pairs) shrink but do not eliminate the blast radius; a correct AI output can still be penalized, which is measurement infidelity in an eval grader. The requirement (must include a case-conversion case) is legit; only the keyword/literal proxy is the defect, matching the finding's scoping.

### `check_trial.py:189` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** An empty-input case is recognized only by the word 'empty' or by two literal '""' occurrences, missing equivalent phrasings.
- **Code:** `empty_case = has_row(u, "empty") or any(row.count('""') >= 2 for row in u)`
- **Concrete failure:** Row `| U2 | blank input yields blank output | edge |` covers the empty-string case but contains no 'empty' and zero '""', so check() reports 'simple U-SEED misses behavior: empty'.
- **Preserve:** Yes — requiring an empty-input case is legit; only the lexical detector is the proxy.
- **Verify:** Confirmed at check_trial.py:189. The empty-input behavior in the "simple" case is detected only by the literal word "empty" or two '""' occurrences in a U-SEED row. The concrete failure genuinely holds: a semantically valid row like `| U2 | blank input yields blank output | edge |` covers the empty-string case but contains neither "empty" nor '""', so check() emits `simple U-SEED misses behavior: empty`. Other legit phrasings (zero-length string, no characters, single-quote '') are also missed. This is a real LEXICAL-FALSE-NEG proxy for the semantic judgment "does the matrix cover empty input?" The underlying decision-coverage requirement is legit and must stay, but acting on the finding only means broadening/making-semantic the detector — that does not gut the coverage requirement or any hashing/E-row/schema/state-closure/snapshot/event-order guard, so no real invariant is damaged. Severity low is apt: it's a frozen known-answer eval harness and only bites on valid-but-nonstandard phrasing.

## eval-mind-explode (8)

### `check_trial.py:245` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Counting '?' characters stands in for the judgment 'the response asks exactly one question.'
- **Code:** `question_marks = output.count("?") + output.count("？") ⏎ if question_marks != 1: ⏎     findings.append(f"conflict response must ask exactly one question; found={question_marks}")`
- **Concrete failure:** A response posing one genuine question but embedding a '?' in inline code or a URL, e.g. "Which delivery guarantee do you need, given the `?dedup=true` flag?" contains two '?' and is wrongly rejected. Conversely a single question spread over two sentences with a trailing '?' passes but a two-part clarification '...A? ...B?' that is really one question is rejected. Same char-count proxy recurs at check_trial.py:277, :351 and check_dag_iteration.py:103, :165.
- **Preserve:** Yes: 'ask exactly one question per turn' is a real wayne-mind-explode behavioral contract. Preserve the single-question intent, but detect actual interrogatives rather than counting '?' glyphs (which fire inside code/URLs/quotes).
- **Verify:** Confirmed at check_trial.py:244-246 and replicated at :276, :350 plus check_dag_iteration.py:12/103/165 (QUESTION = re.compile(r"[?？]")). The check counts '?'/'？' glyphs as a proxy for the semantic judgment 'asks exactly one question'. Glyph count fires inside inline code, URLs, and query-strings, producing a genuine LEXICAL-FALSE-POS: a realistic single clarifying question in the message-queue/dedup domain the tests use — 'Which delivery guarantee do you need, given the `?dedup=true` flag?' — has one question but two '?' glyphs and is wrongly rejected. This polices shape, not meaning, matching the defect class. Fix (detect actual interrogatives) preserves the single-question intent.

### `check_trial.py:249` — MEDIUM · MISCLASSIFIED-ORACLE
- **Proxy:** Keyword presence stands in for the reading-comprehension judgment 'the response identifies which two inputs conflict.'
- **Code:** `if not re.search(r"conflict|incompatible|冲突|矛盾|无法同时|不能同时", output, re.IGNORECASE): ⏎     findings.append("conflict response does not identify the conflicting inputs")`
- **Concrete failure:** "You asked for sub-millisecond writes and a durable fsync on every write; those two goals cannot both hold." precisely names the conflicting inputs but contains none of conflict/incompatible/冲突/矛盾/无法同时/不能同时, so it is wrongly rejected.
- **Preserve:** Yes: the response must identify the conflicting inputs. Preserve that, but whether text identifies a conflict is a contextual reading task a keyword list cannot make.
- **Verify:** Confirmed at line 249: `re.search(r"conflict|incompatible|冲突|矛盾|无法同时|不能同时", output, re.IGNORECASE)` is a pure keyword-presence test proxying for the semantic judgment "the response identifies which two inputs conflict." The concrete failure genuinely holds — "You asked for sub-millisecond writes and a durable fsync on every write; those two goals cannot both hold" names both conflicting inputs and the incompatibility but contains none of the six tokens (English side only catches literal "conflict"/"incompatible"; "cannot both hold", "mutually exclusive", "at odds", "contradictory" all evade it), so it is wrongly flagged. This is a real LEXICAL-FALSE-NEG / MISCLASSIFIED-ORACLE. The isolated string check does not touch the function's real structural guards (check_source_boundary, forbidden-glob file-mutation detection at 240-243, question-count, recommendation), which stay.

### `check_trial.py:354` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Presence of agree/approve/confirm in the question paragraph stands in for 'the agent is asking the user to rubber-stamp its recommendation.'
- **Code:** `question = next((part for part in re.split(r"\n+", output) if "?" in part or "？" in part), "") ⏎ if re.search(r"agree|approve|confirm|同意|批准|确认", question, re.IGNORECASE): ⏎     findings.append("depth response asks for approval of its recommendation")`
- **Concrete failure:** A legitimate substantive clarifying question that happens to use 'confirm', e.g. "Can you confirm the broker is Kafka, or is it RabbitMQ?" (asking for a fact, not approval) is wrongly flagged as approval-seeking. Chinese "请确认消息代理是否为 Kafka？" trips 确认 the same way.
- **Preserve:** Yes: the depth response must not ask the user to merely approve its recommendation. Preserve that, but distinguish approval-seeking from a factual clarification that reuses these words.
- **Verify:** not applicable field placeholder

### `check_trial.py:358` — MEDIUM · SHAPE-POLICING
- **Proxy:** Fixed vocabulary lists (assum/alternative/advantage) stand in for whether the recommendation actually states its assumption, an alternative, and that alternative's benefit.
- **Code:** `if not re.search(r"assum|前提|假设", output, re.IGNORECASE): ⏎     findings.append("recommendation omits its key assumption") ⏎ if not re.search(r"alternative|备选|替代|另一(?:个|种|项)?", output, re.IGNORECASE): ⏎     findings.append("recommendation omits the strongest alternative") ⏎ if not re.search(r"advantage|benefit|优势|好处|更(?:简单|可靠|高效)", output, re.IGNORECASE):`
- **Concrete failure:** "This holds provided the queue stays under capacity. The other option is a partitioned log, which is faster and cheaper." states a premise, an alternative, and its benefit, but matches none of assum/前提/假设 (no 'assum'), none of alternative/备选/替代 ('other option'), and advantage list lacks '更快/cheaper' → three wrongful rejections forcing the author to insert boilerplate keywords.
- **Preserve:** Yes: the recommendation must include its assumption, the strongest alternative, and that alternative's advantage. Preserve those content requirements; the keyword lists reject valid prose that expresses them differently.
- **Verify:** Verified at check_trial.py:358-363. The three checks (`assum|前提|假设`, `alternative|备选|替代|另一...`, `advantage|benefit|优势|好处|更(简单|可靠|高效)`) are fixed vocabulary lists used as a proxy for a semantic content judgment. The concrete example "This holds provided the queue stays under capacity. The other option is a partitioned log, which is faster and cheaper." genuinely states an assumption ("provided..."), an alternative ("the other option"), and its advantage ("faster and cheaper"), yet matches none of the three regexes -> three wrongful rejections. This is a real SHAPE-POLICING / LEXICAL-FALSE-NEG: it forces the author to insert boilerplate keywords (assum/alternative/advantage) instead of expressing the same meaning in natural prose. The content requirement itself is legitimate, but deterministic keyword matching cannot verify it — only a contextual reader can.

### `check_trial.py:364` — MEDIUM · SHAPE-POLICING
- **Proxy:** An 'if/when/unless ... change/choose/prefer/recommend' regex stands in for 'the recommendation states a condition that would reverse it.'
- **Code:** `if not re.search( ⏎     r"(?:if|when|unless).{0,120}(?:change|choose|prefer|recommend)|" ⏎     r"改变建议|会改变建议|反转条件|" ⏎     r"(?:如果|若|当|除非).{0,120}(?:改变|改推|改选|推荐|转向)", ⏎     output, ⏎     re.IGNORECASE | re.DOTALL, ⏎ ): ⏎     findings.append("recommendation omits a reversal condition")`
- **Concrete failure:** "Should throughput exceed 1M msg/s, I'd switch to partitioned queues instead." is a clear reversal condition but uses 'Should' (not if/when/unless) and 'switch' (not change/choose/prefer/recommend), so it is wrongly rejected.
- **Preserve:** Yes: the recommendation must state a reversal condition. Preserve that, but the fixed connective+verb regex only accepts one phrasing of the same idea.
- **Verify:** Verified at check_trial.py:364-371 in validate_depth_recommendation. The regex requires a conditional connective (if/when/unless or 如果/若/当/除非) AND, within 120 chars, one of a narrow verb set (change/choose/prefer/recommend / 改变/改推/改选/推荐/转向) to certify that "the recommendation states a condition that would reverse it." I tested the concrete failure: "Should throughput exceed 1M msg/s, I'd switch to partitioned queues instead." returns None because "Should" is not in the connective set and "switch" is not in the verb set. Other clear reversal conditions ("Once latency drops below 5ms, go with the alternative", "In the event of multi-region traffic, the alternative wins") also fail. This is SHAPE-POLICING / MISCLASSIFIED-ORACLE: a lexical connective+verb pattern standing in for a semantic judgment only a contextual reader can make, producing a real false negative that would spuriously fail a legitimate agent trial. The semantic requirement itself is fine; the mechanism only accepts one phrasing.

### `check_dag_iteration.py:107` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** A keyword regex (e.g. topology = topolog|inline|拓扑|内联) stands in for 'this turn's question is about the correct DAG frontier node.'
- **Code:** `if not TERMS[("topology", "semantics", "retry")[turn - 1]].search(output): ⏎     findings.append(f"turn {turn} asks about the wrong DAG node")`
- **Concrete failure:** Turn 1 legitimately asks the topology question phrased as "Should the dispatcher run in-process or as a separate service?" — this is exactly the topology node, but matches neither 'topolog' nor 'inline'/'内联', so it is wrongly reported as asking about the wrong node. The parallel check_dag_iteration.py:169 keyword list (operator|manual|replay...) has the same weakness.
- **Preserve:** Yes: each turn must ask about the correct frontier node. Preserve that ordering invariant; matching whether the question addresses the node is a semantic call the keyword set gets wrong for valid rephrasings.
- **Verify:** Confirmed at line 107. The check searches free-text output with TERMS['topology'] = `topolog|inline|拓扑|内联` to decide whether "turn 1 asks about the correct DAG node." The concrete failure holds: "Should the dispatcher run in-process or as a separate service?" is precisely the topology/deployment decision but contains none of the tokens ("in-process" is not a substring of "inline"), so it is wrongly flagged as "asks about the wrong DAG node." This is a lexical proxy for a semantic call — a valid rephrasing of the exactly-correct question is rejected because it doesn't echo the harness's vocabulary. The parallel line 169 (`operator|manual|replay|recovery|人工|恢复|重放`) shares the weakness (e.g. "who handles a message that permanently fails after the last attempt?" matches nothing). It is a real LEXICAL-FALSE-NEG / shape-policing defect.

### `check_trial.py:190` — LOW · SHAPE-POLICING
- **Proxy:** Requiring four literal English tokens stands in for 'the spec contains a cybernetics analysis.'
- **Code:** `for term in ("Plant", "Controller", "Setpoint", "Feedback"): ⏎     if term not in spec_text: ⏎         findings.append(f"cybernetics analysis omits {term}")`
- **Concrete failure:** A correct cybernetics analysis that writes 'set point' (two words) fails the 'Setpoint' token; one using the standard Chinese control-theory terms (被控对象 / 控制器 / 设定值 / 反馈) or synonyms like 'plant model' vs 'the plant' passes/fails on spelling, not substance. 'Setpoint' spelled 'set-point' is wrongly rejected.
- **Preserve:** Yes: the spec must include a cybernetics analysis. Preserve that requirement; the four-token substring test polices vocabulary spelling rather than the analysis.
- **Verify:** Confirmed at check_trial.py:190-192: a case-sensitive substring test (`term not in spec_text`) for four literal tokens stands in for "the spec contains a cybernetics analysis" — a completeness judgment only a contextual reader can make (SHAPE-POLICING / lexical proxy). The concrete false-negative genuinely holds because the fixture names the controller `Dispatcher`, so a complete, correct analysis naturally writes lowercase "controller", "plant", and two-word/hyphenated "set point"/"set-point" — none of which contain the capitalized substrings "Controller"/"Plant"/"Setpoint", producing spurious "cybernetics analysis omits X" findings and failing a correct run. It polices vocabulary spelling/casing, not the presence of the analysis. (The finding's Chinese-terms scenario is weak — fixtures are English-only per Wayne convention — but the casing/spelling false-neg is real.) It is NOT a protected invariant guard: relaxing it to case-insensitive or a real presence heuristic loses no hashing, E-row carry, decision-coverage, schema, or event-order correctness; the legit sibling checks in the same function are independent. Severity corrected to low because the skill contract (SKILL.md:187) explicitly prescribes these capitalized terms, so compliant runs usually pass and friction is bounded.

### `check_trial.py:281` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Keyword presence stands in for 'the response routes the user toward design-section approval.'
- **Code:** `if not re.search(r"design|architecture|section|设计|架构|章节|批准|确认", output, re.IGNORECASE): ⏎     findings.append("decision-lock response did not route to design approval")`
- **Concrete failure:** A response that routes correctly using different words, e.g. "The decision is locked; next you need to sign off on the layered structure before I proceed." contains none of design/architecture/section/设计/架构/章节/批准/确认 and is wrongly rejected. ('design'/'section' are broad enough that most outputs pass, so lower severity, but the proxy still misfires on valid routing prose.)
- **Preserve:** Yes: the response must route to design approval. Preserve the routing requirement; the disjunction of common words is a shape proxy for that intent.
- **Verify:** Confirmed at check_trial.py:281. The check `re.search(r"design|architecture|section|设计|架构|章节|批准|确认", output)` is a lexical disjunction standing in for the semantic judgment "does this response route the user to design-section approval." Concrete false-neg verified: "The decision is locked; next you need to sign off on the layered structure before I proceed." routes correctly but matches none of the 8 keywords, so it is wrongly rejected. This is a genuine LEXICAL-FALSE-NEG / shape proxy. It does NOT guard a real structural invariant: the actual state guards (decision-log markers at lines 260-265, forbidden-artifact globs at 271-274) are separate and would be preserved; line 281 only polics free-text word choice. Severity low is correct because 'design'/'section' are broad enough that most conforming outputs pass — the proxy only misfires on valid routing prose using synonyms.

## validate_plan (7)

### `validate_plan.py:682` — MEDIUM · SHAPE-POLICING
- **Proxy:** An exact-two-arrow count stands in for the judgment 'is this scenario a concrete input→action→expected result'.
- **Code:** `if scenario.count("→") != 2 or any(phrase.lower() in scenario.lower() for phrase in BANNED):`
- **Concrete failure:** A clear two-branch scenario 'OFF → disabled; ON → enabled' (4 arrows) is rejected, and a clear single-clause scenario 'missing config file → raises ConfigError' (1 arrow) is also rejected — both are perfectly concrete and readable, but neither has exactly two '→'.
- **Preserve:** The BANNED half of this same expression legitimately catches TBD/TODO-style placeholder scenarios; that half plus the concreteness *intent* should be kept — only the count()==2 template-shape enforcement is the defect.
- **Verify:** Confirmed at line 682. The check is an OR: `scenario.count("→") != 2` (shape proxy) OR any BANNED placeholder phrase. The arrow-count half enforces a fixed two-arrow template as a stand-in for the uncountable judgment "is this scenario concrete." Concrete real inputs are wrongly rejected: `missing config file → raises ConfigError` (1 arrow) and a legit two-branch `missing config → load() → ConfigError; valid config → load() → dict` (4 arrows) both fire the rejection though both are clear and contain no banned placeholder. This is a genuine LEXICAL/SHAPE-POLICING false-neg. (Caveat: the finding's own example `OFF → disabled; ON → enabled` has exactly 2 arrows and would actually PASS, so that specific instance is mis-stated — but the defect holds via 1-arrow and 4-arrow cases.) The BANNED half is a legit separable placeholder guard and must be kept.

### `validate_plan.py:371` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A substring scan for descriptive phrases stands in for the judgment 'is this field vague/placeholder text'.
- **Code:** `for phrase in BANNED: ⏎     if phrase.lower() in lowered: ⏎         findings.add("placeholder", f"{unit_id} {field_name} contains {phrase!r}")`
- **Concrete failure:** A Goal of 'Add validation for the email field using regex R5' is flagged because it contains the substring 'add validation'; a Technical design saying 'Produces output similar to Unit I2's schema' is flagged for 'similar to Unit' — both are concrete, specific statements, not placeholders.
- **Preserve:** The literal-marker entries in BANNED (TBD, TODO, 'implement later') are genuine placeholder tokens and must stay; only the prose-phrase entries ('add validation', 'add error handling', 'handle edge cases', 'write tests', 'similar to Unit') over-fire on legitimate concrete prose.
- **Verify:** Confirmed at validate_plan.py:370-372: `for phrase in BANNED: if phrase.lower() in lowered` does a raw case-insensitive substring scan over each unit field. BANNED (lines 48-57) mixes genuine literal placeholder markers (TBD, TODO, implement later) with prose phrases (add validation, add error handling, handle edge cases, write tests, similar to Unit). The substring approach over-fires on concrete prose: a Goal of "Add validation for the email field using regex R5" contains "add validation" and is flagged; a design note "Produces output similar to Unit I2's schema" contains "similar to unit" and is flagged. No code-stripping, word-boundary, or context guards prevent this — both concrete failures genuinely hold. This is a textbook LEXICAL-FALSE-POS: a substring proxy for the semantic judgment "is this placeholder/vague text," penalizing legitimate specific prose. Correct fix keeps the literal markers and drops/refines the prose phrases.

### `validate_plan.py:897` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A 'backtick then slash' regex stands in for the judgment 'this is an absolute filesystem path'.
- **Code:** `if re.search(r"`(?:/|[A-Za-z]:\\)[^`]+`", body):`
- **Concrete failure:** A plan that documents a REST route in inline code, e.g. `/api/tasks`, or a regex pattern `/foo/`, is flagged as 'contains an absolute path' even though it is a URL/route/pattern, not a filesystem path.
- **Preserve:** The intent 'no absolute filesystem paths in a portable plan' is legitimate; a fix must keep that but exclude URL routes / regex / non-fs slashes rather than flag any backtick-slash content.
- **Verify:** Verified at validate_plan.py:897. Regex `` `(?:/|[A-Za-z]:\\)[^`]+` `` matches any inline-code span starting with '/'. It fires on REST routes like `/api/tasks` (used throughout this project's own docs) and regex literals like `/foo/`, flagging them as 'plan contains an absolute path' though they are URLs/patterns, not filesystem paths. It is a pure lexical 'backtick-then-slash' proxy for the semantic judgment 'is an absolute fs path', forcing worse prose to satisfy it — a genuine LEXICAL-FALSE-POS.

### `validate_plan.py:300` — MEDIUM · MISCLASSIFIED-ORACLE
- **Proxy:** A word-boundary grep for the symbol's last token stands in for 'this symbol is actually defined in that file' — a call only a language-aware reader can make.
- **Code:** `token = re.split(r"[.:-]", symbol)[-1] ⏎ if not re.search(rf"\b{re.escape(token)}\b", text): ⏎     findings.add(code, f"symbol {symbol!r} was not found in {relative}")`
- **Concrete failure:** Surface `foo.py::calculate` passes when 'calculate' appears only in a comment or unrelated string and is never defined (false accept); conversely a dynamically/metaprogrammatically defined symbol, or a qualified surface like `mod.py::Cfg.render_html` (token='render_html') that exists only via a generated attribute, is rejected as 'not found' (false reject). Only the last dotted/dashed segment is even checked, so `A.b` and `X.b` are indistinguishable.
- **Preserve:** Verifying that referenced repository surfaces exist is a real invariant worth keeping; the defect is the loose lexical proxy (last-token grep anywhere in the file), not the existence check itself.
- **Verify:** Confirmed at lines 299-301. `token = re.split(r"[.:-]", symbol)[-1]` reduces a symbol to its last dotted/colon/dashed segment, then `re.search(rf"\b{token}\b", text)` does a bare word-boundary grep over the entire file. This is a lexical proxy for the semantic claim "this symbol is defined in this file" — a call only a language-aware reader can make. False accepts hold: `foo.py::calculate` passes when `calculate` appears only in a comment/string and is never defined. False rejects hold: a metaprogrammatically/generated attribute (e.g. render_html via setattr) never appears literally and is flagged not-found; and `A.b` vs `X.b` both collapse to token `b`, so they are indistinguishable. This is a genuine MISCLASSIFIED-ORACLE. The lexical part flagged is only the grep; the file.is_file()/UTF-8 checks are the real existence invariant and stay untouched.

### `validate_plan.py:895` — LOW · LEXICAL-FALSE-POS
- **Proxy:** 'A line beginning with the word git' stands in for 'this is an executable git command'.
- **Code:** `if re.search(r"(?m)^\s*(?:\$\s*)?git\s+\S+", body):`
- **Concrete failure:** A prose paragraph line 'git worktrees isolate this change, so we rely on them.' or 'git history confirms the earlier rename.' is flagged as 'contains a git command' — it is ordinary explanatory prose that merely starts with the lowercase word 'git'.
- **Preserve:** Keeping executable command lines out of a descriptive plan is a legitimate intent; a fix should require command-shaped context (e.g. code fence / $ prompt) rather than any line starting with 'git '.
- **Verify:** Confirmed at line 895: re.search(r"(?m)^\s*(?:\$\s*)?git\s+\S+", body) flags any line beginning with the lowercase word 'git' + a word. Prose like 'git worktrees isolate this change, so we rely on them.' or 'git history confirms the earlier rename.' matches (git worktrees / git history) and is wrongly reported as 'plan contains a git command'. This is a genuine LEXICAL-FALSE-POS: 'line starts with git' is a shape proxy for 'executable git command.' The scenario is realistic — wayne plans routinely describe git-worktree isolation, and 'git' is conventionally lowercase. It's narrow (case-sensitive, so 'Git' is safe; needs git at column 0, so '- git ...' bullets are safe), which fits the low severity. The correct fix (require command-shaped context: code fence or $ prompt) preserves the legitimate 'keep commands out of a descriptive plan' intent.

### `validate_plan.py:741` — LOW · LEXICAL-FALSE-POS
- **Proxy:** A whole-section substring scan for done/fail glyphs stands in for 'a status column contains a downstream-only status'.
- **Code:** `if any(status in section for status in ("☑", "✅", "❌")):`
- **Concrete failure:** A U-row whose Scenario legitimately describes rendering a checkmark, e.g. 'GET /health → body contains ✅', is flagged as a 'downstream-only Test Matrix status' even though the actual Status column is the required ☐ — the glyph is content, not a status.
- **Preserve:** The invariant 'a pre-execution plan carries no completed statuses' is real and must stay; the defect is scanning the entire section text instead of only the status column(s).
- **Verify:** The proposed fix is to narrow the glyph scan to the status column(s), not delete it. The "pre-execution plan carries no completed status" invariant is already guarded per-row at line 684 for U-rows and the E-contract is carried verbatim; narrowing 741's scope preserves that invariant while removing the false-positive, so acting on this finding does not damage any protected guard (no hashing/E-carry/decision-coverage/schema/state-closure/mutation/snapshot/event-order guard is gutted).

### `validate_plan.py:893` — LOW · SHAPE-POLICING
- **Proxy:** The presence of a runnable-language code-fence tag stands in for 'this plan contains implementation code rather than a description'.
- **Code:** `if re.search(r"```(?:python|javascript|typescript|tsx|jsx|java|go|rust|ruby|bash|sh|shell)\b", body, re.IGNORECASE):`
- **Concrete failure:** A plan that includes a one-line illustrative interface signature fence tagged ```python (e.g. showing `def sync(repo: Path) -> int`) to communicate a Produces surface is rejected as 'contains a runnable-language code fence', forcing the author to strip the language tag or degrade the signature into worse prose.
- **Preserve:** The intent 'a plan describes, it does not implement' is a deliberate contract; if kept, it polices fence tags rather than whether the fenced content is genuinely an implementation, so short illustrative snippets are false-flagged.
- **Verify:** Confirmed at line 893: the regex matches only the code-fence LANGUAGE TAG (python|javascript|...), never the fenced content, so it uses a lexical/mechanism proxy for the semantic judgment 'is this implementation code vs. a descriptive surface'. Concrete false-positive holds: a plan showing a one-line Produces-surface signature in a ```python fence (e.g. `def sync(repo: Path) -> int`) is rejected as 'runnable-language code fence', identically to a full 50-line implementation the regex cannot tell them apart. This is the same lexical-content-policing family as the already-known false-pos at line 897 (absolute-path regex). Severity is low, not medium/high, because an untagged ``` fence or inline backticks trivially avoids the trigger, so friction is real but the 'degrade into worse prose' framing is overstated. It is NOT one of the protected guard categories (hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, exact-snapshot, event-order), so loosening it damages no real invariant.

## eval-global-instructions (7)

### `check_instructions.py:17` — MEDIUM · SHAPE-POLICING
- **Proxy:** The literal phrase 'no git commit/branch unless explicitly asked' (with the slash idiom) stands in for the judgment 'does this doc forbid unauthorized commits/branches'.
- **Code:** `"no implicit commit or branch": ( ⏎     r"no git commit/branch unless explicitly asked", ⏎ ),`
- **Concrete failure:** An instruction doc that says 'Never create a commit or a branch without explicit user approval' expresses the exact same rule but fails the regex — no substring 'commit/branch' and different wording. This whole file is systemic: it verifies wording presence, not that the meaning is conveyed.
- **Preserve:** The real invariant ('the candidate instruction doc actually conveys this rule') is genuinely worth checking, but it is semantic and a substring match cannot verify it; there is no structural sub-part to preserve here.
- **Verify:** Confirmed against the code: check() (line 78) runs re.search of the literal phrase "no git commit/branch unless explicitly asked" against the candidate doc, case-insensitive but otherwise an exact substring test. The concrete failure holds: a doc reading "Never create a commit or a branch without explicit user approval" expresses the identical rule yet shares none of the required literal tokens (no "commit/branch" slash idiom, different wording) and would be reported as "missing". This is a genuine LEXICAL-FALSE-NEG / SHAPE-POLICING: the check verifies presence of one exact wording, not the semantic invariant it claims ("doc conveys the no-unauthorized-commit rule"). The problem is systemic to the whole CONTRACTS dict, but line 17 is a clean single-phrase instance with no structural sub-part to preserve.

### `check_instructions.py:59` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Requires the literal token 'Occam' plus two specific sentence skeletons to prove the doc teaches parsimonious root-cause convergence.
- **Code:** `"Occam RCA convergence": ( ⏎     r"Occam", ⏎     r"search heuristic[^\n]{0,100}(?:NOT|not)[^\n]{0,30}stop condition", ⏎     r"explains ALL observations[^\n]{0,80}reproduces[^\n]{0,80}sibling paths", ⏎ ),`
- **Concrete failure:** A doc that teaches 'prefer the simplest hypothesis that explains every symptom, and treat parsimony as a ranking heuristic, not a reason to stop investigating' conveys the principle but omits the word 'Occam' and the exact 'sibling paths' phrasing, so it fails all three.
- **Preserve:** none (semantic; no structural part)
- **Verify:** Verified at check_instructions.py:58-62. The check() function (lines 76-79) requires all three patterns to match via re.search. The contract is labeled "Occam RCA convergence" — a semantic property (does the doc teach parsimonious root-cause convergence) — but proves it via a literal token ("Occam") plus two highly specific prose skeletons ("search heuristic … not … stop condition"; "explains ALL observations … reproduces … sibling paths"). A doc that teaches the identical principle in different words (e.g. "prefer the simplest hypothesis that explains every symptom, and treat parsimony as a ranking heuristic, not a reason to stop investigating") fails all three patterns despite being semantically valid. This is a genuine LEXICAL-FALSE-NEG / shape-policing: a lexical proxy standing in for a semantic judgment regex cannot make, and it forces authors into templated phrasing. Notably more fragile than sibling contracts (e.g. delete > add) because of its unusual multi-token skeletons.

### `check_instructions.py:12` — MEDIUM · SHAPE-POLICING
- **Proxy:** Proximity regexes require the tokens 'chat'..'Chinese' and 'output/write'..'English' near each other, standing in for 'the doc states the language split'.
- **Code:** `"Chinese chat and English files": ( ⏎     r"chat[^\n]{0,50}Chinese", ⏎     r"(?:output|write)[^\n]{0,60}(?:files|code|docs|configs?)[^\n]{0,40}English", ⏎ ),`
- **Concrete failure:** A doc saying 'Reply to the user in Chinese; everything committed to the repository stays in English' states the rule exactly but fails: 'chat' never appears, and 'committed to the repository' is not one of the allowed nouns (files/code/docs/configs) within 60 chars of English.
- **Preserve:** none (semantic; no structural part)
- **Verify:** Confirmed. The contract at check_instructions.py:12-15 requires two lexical-proximity matches to prove the semantic claim "the doc states the Chinese-chat / English-files split." Regex 1 (chat[^\n]{0,50}Chinese) demands the literal token 'chat'; Regex 2 demands 'output'/'write' near a noun from {files,code,docs,configs} near 'English'. The input 'Reply to the user in Chinese; everything committed to the repository stays in English' states the exact rule but fails both: no 'chat' token, and 'committed to the repository' is not an allowed noun within range. This is a genuine LEXICAL-FALSE-NEG / SHAPE-POLICING proxy that rejects semantically valid phrasings and forces the author to use specific vocabulary. It guards no structural invariant — check() only runs presence-regex over free prose.

### `check_trial.py:166` — MEDIUM · MISCLASSIFIED-ORACLE
- **Proxy:** Presence of any `while`/`async for` node is treated as proof of polling, but poll-vs-push is a semantic distinction the AST cannot make.
- **Code:** `if any(isinstance(node, (ast.While, ast.AsyncFor)) for node in ast.walk(tree)): ⏎     findings.append("watcher still polls with a loop")`
- **Concrete failure:** A textbook push implementation `async def _consume(self):\n    async for value in self._source.stream():\n        self.value = value` (or `while True: value = await self._queue.get(); self.value = value`) is pure event-driven consumption — asyncio Queue / async-iterator over an event stream, exactly the reactive primitive the instructions endorse — yet it is rejected as 'still polls'.
- **Preserve:** Yes — the 'no polling' invariant. But it is already proven functionally by the emit/observe probe at lines 172-193; keep that probe and drop the AST shape check, which forbids valid push loops.
- **Verify:** Confirmed at check_trial.py:166. The check flags ANY ast.While/ast.AsyncFor node as "watcher still polls with a loop" — a mechanism/shape proxy that cannot distinguish poll from push. Both concrete inputs genuinely fail: `while True: value = await self._queue.get(); self.value = value` (asyncio Queue consumer) and `async for value in self._source.stream()` (async-iterator over an event stream) are idiomatic event-driven consumption, and CLAUDE.md's own "Push, Don't Poll" section explicitly endorses asyncio Queue as a valid reactive primitive — yet a queue consumer inherently needs a loop, so the endorsed idiom is rejected. The real "no poll" invariant is already carried functionally by the emit/observe probe at lines 172-193 (emit "new" with no delay, assert watcher.value == "new"): a true polling impl would not observe the emit synchronously. So the AST loop check is a redundant shape gate that produces false negatives. MISCLASSIFIED-ORACLE / SHAPE-POLICING is the right classification.

### `check_instructions.py:36` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** The exact idiom 'touch only what you must' proxies for the surgical-scope principle.
- **Code:** `"surgical scope": (r"touch only what you must",),`
- **Concrete failure:** A doc stating 'Make the smallest change necessary; do not modify adjacent code, comments, or formatting' fully encodes surgical scope but is rejected because the fixed idiom is absent.
- **Preserve:** none (semantic; no structural part)
- **Verify:** Line 36 `"surgical scope": (r"touch only what you must",)` is a single fixed-idiom regex used by check() (line 78) as a presence proxy for the surgical-scope principle. It genuinely rejects semantically valid rephrasings: a doc saying 'Make the smallest change necessary; do not modify adjacent code/comments/formatting' encodes the principle fully but yields FAIL because the exact idiom is absent (LEXICAL-FALSE-NEG / SHAPE-POLICING). Presence of the idiom is not equivalent to presence of the meaning (it also passes a negated mention). Unlike sibling contracts in the file (lines 22, 27) it has no alternation, so it is the brittle single-string variant the defect hunt targets.

### `check_trial.py:221` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Due to alternation precedence, `^` anchors only 'Co-Authored-By:'; 'Robot' and 'noreply' match anywhere in the body, standing in for 'has a bot trailer'.
- **Code:** `if re.search(r"(?im)^Co-Authored-By:|Robot|noreply", body): ⏎     findings.append("commit contains a bot/co-author trailer")`
- **Concrete failure:** A legit commit body `[why]\n- remove the hardcoded noreply@ fallback address\n[how]\n- read the sender from env` is flagged as a bot/co-author trailer because 'noreply' appears in the actual subject matter. Likewise a commit about 'robotics' matches 'Robot' (case-insensitive substring of 'robotics').
- **Preserve:** Yes — must reject real `Co-Authored-By:` / bot-identity trailers. Preserve the anchored trailer check; the free-floating 'Robot'/'noreply' word match is the defect (should be anchored to a trailer line, e.g. author/email context).
- **Verify:** Confirmed at check_trial.py:221. Regex `(?im)^Co-Authored-By:|Robot|noreply`: alternation precedence anchors `^` to only the first arm, so `Robot`/`noreply` are unanchored case-insensitive substring matches. Runtime-verified false positives: a commit body containing prose 'noreply@ fallback' matches on 'noreply', 'robotics slug parser' matches on 'robot', and a diligent 'avoided noreply/Robot identity' body is also flagged — all while the check is supposed to mean 'has a bot co-author trailer'. This is a genuine LEXICAL-FALSE-POS: it policies substring appearance rather than a trailer. Only the free-floating word arms are the defect; the anchored `^Co-Authored-By:` trailer detection is legit and must stay.

### `check_trial.py:170` — LOW · SHAPE-POLICING
- **Proxy:** Any method call whose attribute is named 'sleep' is treated as poll-loop sleeping.
- **Code:** `if node.func.attr == "sleep": ⏎     findings.append("watcher still sleeps between polls")`
- **Concrete failure:** An event-driven watcher that does `await asyncio.sleep(0.05)` as a debounce AFTER receiving an emitted event (still push, not polling) is flagged 'still sleeps between polls'. An unrelated domain call `self.animation.sleep()` would also match.
- **Preserve:** Yes — discourages sleep-based polling, but the emit/observe probe already proves push behavior; the attr-name match cannot tell a poll-sleep from a legit debounce.
- **Verify:** Confirmed at check_trial.py:168-171: the AST walk flags ANY call whose attribute name is "sleep" and emits "watcher still sleeps between polls" without proving a poll exists. This is a genuine SHAPE-POLICING / LEXICAL-FALSE-POS: `await asyncio.sleep(0.05)` used as a post-event debounce (still push) and an unrelated `self.animation.sleep()` both match purely by name. The message's "between polls" claim is unearned — the attr-name match cannot distinguish a poll-sleep from a legit debounce. guts_real_invariant is false because the real push invariant is already proven by two other guards that survive removal of this check: (a) the emit/observe probe at lines 172-193 uses an immediate synchronous assert (`source.emit("new"); assert watcher.value == "new"`) which no poller can satisfy, and (b) the loop check at line 166 catches busy-poll While/AsyncFor loops. The sleep attr-match is a redundant lexical layer, not the sole guardian of the invariant, so removing it loses no correctness. Severity low: the canonical correct push watcher has no sleep, so the false-positive surface is narrow but real.

## validate_skill (5)

### `validate_skill.py:121` — MEDIUM · MISCLASSIFIED-ORACLE
- **Proxy:** The mere co-existence of a `## Flow` heading and a `## Checklist` heading stands in for the semantic judgment that the checklist restates the flowchart's sequence.
- **Code:** `if has_flow and re.search(r"^##\s+Checklist\s*$", body, re.MULTILINE): ⏎     findings.append(finding("error", "flow-duplicate", "Checklist must not restate Flowchart sequence"))`
- **Concrete failure:** A SKILL.md with a `## Flow` dot DAG of the execution steps AND a `## Checklist` of independent preconditions ("repo clean, tests green, ticket exists") is rejected as "Checklist must not restate Flowchart sequence" and exits 1, even though the checklist covers entirely different content. The author is forced to delete or rename a legitimate section.
- **Preserve:** none — the presence of two differently-named sections is not an invariant; only a contextual reader can tell whether the checklist actually duplicates the flow's ordered steps.
- **Verify:** Confirmed at validate_skill.py:121. The check fires `flow-duplicate` (error, exits 1) solely when exact `## Flow` and `## Checklist` level-2 headings co-exist; it never compares the checklist content to the flowchart steps, yet its message asserts "Checklist must not restate Flowchart sequence" — a semantic-equivalence claim only a contextual reader can make. Concrete false-neg: a SKILL.md with a Flow dot DAG plus a Checklist of independent preconditions (repo clean, tests green, ticket exists) is rejected even though it duplicates nothing. This is a genuine MISCLASSIFIED-ORACLE / lexical proxy, not a structural invariant: actual duplicate headings are already caught separately at lines 111-113 (duplicate-heading), so this check guards nothing real.

### `validate_skill.py:112` — MEDIUM · SHAPE-POLICING
- **Proxy:** A raw Counter over ALL heading text at every level (H1-H6) treats any repeated heading string as an error, using text-equality as a proxy for accidental copy-paste duplication.
- **Code:** `duplicates = sorted(h for h, count in Counter(headings).items() if count > 1) ⏎ if duplicates: ⏎     findings.append(finding("error", "duplicate-heading", f"duplicate headings: {duplicates}"))`
- **Concrete failure:** A skill documenting two modes, each with an `### Example` subheading (or two `### Notes`, or `### Steps` under different parent sections) — perfectly valid, unambiguous markdown — is rejected with an error and exits 1. It forces the author to write worse headings like `### Example (create mode)` purely to satisfy the count.
- **Preserve:** yes — catching an entire duplicated top-level (H2) section is a real structural concern; keep a check scoped to same-level structural sections, but the across-all-levels identical-short-subheading form is the lexical over-reach to drop.
- **Verify:** Confirmed at validate_skill.py:110-113. The check does `Counter(re.findall(r"^#{1,6}\s+(.+?)\s*$", body))` and raises an `error` for any text-identical heading repeated across ALL levels H1-H6. Concrete false-positive holds: a SKILL.md documenting two modes each with a `### Example` (or `### Notes`, `### Steps`) subheading under different parent sections is valid, unambiguous markdown, yet is rejected with an error. It uses text-equality as a proxy for accidental copy-paste, forcing worse headings like `### Example (create mode)` to satisfy a count. This is genuine SHAPE-POLICING/lexical over-reach. It does not guard any protected invariant (not hashing/schema/state-closure/decision-coverage/exact-snapshot/event-order); the legit intent (catching a fully duplicated top-level section) can be preserved by scoping to same-level H2 duplication without gutting anything.

### `validate_skill.py:107` — LOW · LEXICAL-FALSE-POS
- **Proxy:** A heading-title regex (also line 105 `## Inherits from`) stands in for the semantic judgment that a section contains routing / global-invariant content.
- **Code:** `if re.search(r"^##\s+When to Run\b", body, re.MULTILINE | re.IGNORECASE): ⏎     findings.append(finding("error", "when-to-run", "routing belongs in frontmatter description"))`
- **Concrete failure:** A section titled `## When to Run Diagnostics` that explains at which pipeline step to invoke a diagnostic sub-step (legit body content, not skill-selection routing) matches because of the `\b` after "Run" and is rejected as "routing belongs in frontmatter description." Likewise `## Inherits from the base template` (describing template inheritance, not global invariants) is wrongly rejected at line 105.
- **Preserve:** the intent (don't duplicate routing/global invariants in the body) is real, but it can only be judged from section content, not the heading string; no invariant is preserved by the title-match form.
- **Verify:** Confirmed code at line 107 matches exactly: re.search(r"^##\s+When to Run\b", body, MULTILINE|IGNORECASE) -> error "routing belongs in frontmatter description" (and line 105 mirror for "## Inherits from\b"). This is a genuine LEXICAL-FALSE-POS / MISCLASSIFIED-ORACLE: the check matches a heading string to infer that a section contains routing content, a judgment that can only be made from body content. The concrete false positive holds — because the pattern ends in \b (not $), `## When to Run Diagnostics` (an h2 documenting when to invoke an internal diagnostic step, i.e. legit operational body content, not skill-selection routing) matches at the word boundary between "n" and the space, and is rejected at error level. No hard invariant is preserved by the title-match form. Severity lowered to low because the trigger surface is narrow: only h2 headings match (^##\s+ won't match ### since the next char is #), only headings literally beginning "When to Run"/"Inherits from" fire, and the workaround is a trivial rename. It is still a real blocking (error, not warning) false positive, so not zero-impact.

### `validate_skill.py:157` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** A bare substring test (`relative in body` OR `name in body`) stands in for whether a bundled resource is actually referenced/used by the skill.
- **Code:** `if relative not in body and resource.name not in body: ⏎     findings.append(finding("warning", "unreferenced-resource", f"resource is not named: {relative}"))`
- **Concrete failure:** A dead, unused file `references/faq.md` produces NO warning if the body contains the string `faq.md` anywhere — e.g. inside an unrelated URL `https://example.com/help/faq.md` — because the basename substring matches (false negative). Conversely, a template `templates/report.md` that IS used by the skill's script but is only described in prose ("generates the report") without its exact filename triggers a spurious "unreferenced" warning (false positive).
- **Preserve:** yes — flagging dead/unreferenced bundled resources is a real Delete>Add concern; keep the folder-walk, but replace the raw substring proxy with actual link/reference resolution.
- **Verify:** Acting on this finding (replace substring test with real link/reference resolution while keeping the folder-walk) touches none of the protected guards — no hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, snapshot, or event-order. It is a non-blocking warning; the structural broken-link check at 143-148 that does real resolution is a separate code path and remains intact.

### `validate_skill.py:129` — LOW · SHAPE-POLICING
- **Proxy:** A literal `shape=doublecircle` attribute substring stands in for the structural fact that the flowchart has a terminal/end state.
- **Code:** `if not any("shape=doublecircle" in attrs.replace(" ", "") for attrs in nodes.values()): ⏎     findings.append(finding("error", "dot-terminal", f"dot block {index} has no terminal node"))`
- **Concrete failure:** A valid dot flow whose end state is denoted with `[peripheries=2]` or `[shape=circle,style=bold,label="done"]` (both standard Graphviz ways to mark a terminal) is rejected as "has no terminal node" and exits 1, forcing the author to restyle a correct diagram to a specific attribute string.
- **Preserve:** yes — requiring a flowchart to have a terminal/end node is a legit DAG-closure structural check; keep the concept but detect terminality structurally (a node with no outgoing edges) rather than by the exact attribute string.
- **Verify:** Confirmed at validate_skill.py:129. The check `"shape=doublecircle" in attrs.replace(" ", "")` is a literal-substring proxy for the structural fact "the flowchart has a terminal node." A valid, semantically-identical terminal written as `[shape=circle, peripheries=2]` (Graphviz's doublecircle IS circle+peripheries=2) becomes `shape=circle,peripheries=2` after the space-strip and does NOT match, so it is rejected as "has no terminal node" and exits 1. The concrete false-negative genuinely holds. The whole DOT loop uses substring matching (shape=diamond at 131, label= at 138) and never inspects structure, so this policies the attribute string rather than the meaning it carries — a genuine SHAPE-POLICING lexical false-neg. Low severity: real alternative encodings are rejected, but most Wayne authors follow the doublecircle convention, so friction is limited and there are no false-positives.

## eval-checkpoint (5)

### `check_trial.py:110` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A verb+agent-name regex stands in for the judgment 'did the result falsely claim it auto-ran the downstream agent', but it cannot distinguish a claim of self-invocation from the correct instruction telling the user to run it manually.
- **Code:** `if re.search(r"\b(?:invoke|run|start)(?:d|s|ing)?\s+" + re.escape(expected_agent) + r"\b", output, re.IGNORECASE): ⏎     findings.append("user-visible result claims downstream invocation")`
- **Concrete failure:** output = 'Handoff ready. To proceed, manually run wayne-work when you are ready.' — this is exactly the correct manual-handoff behavior, but 'run wayne-work' matches the regex and is flagged as 'user-visible result claims downstream invocation'.
- **Preserve:** Yes — it guards the real invariant that the result must NOT claim it auto-invoked/auto-advanced the next agent (Wayne never auto-advances). That invariant must be preserved; only the over-broad verb+name proxy that also catches 'you should run X' is the defect.
- **Verify:** Confirmed at check_trial.py:110. The regex `\b(?:invoke|run|start)(?:d|s|ing)?\s+wayne-<agent>\b` over `output` cannot distinguish an imperative instruction to the user ("manually run wayne-work when ready") from a first-person false claim of self-invocation ("I ran wayne-work"). Lines 112 and 114 REQUIRE the output to contain both the agent name and the word "manual", so the natural correct handoff phrasing "To proceed, manually run wayne-work" satisfies those two required checks yet its substring "run wayne-work" trips line 110 and is flagged as "claims downstream invocation." That is a real LEXICAL-FALSE-POS on legitimately correct output. guts_real_invariant=false: the guarded invariant is a semantic "did-it-falsely-claim-auto-advance" judgment, not one of the protected structural guards (hash/E-row/decision-coverage/schema/state-closure/file-mutation/snapshot/event-order); the manual/auto-advance-NO semantics are already carried structurally by lines 108/112/114, so narrowing this lexical proxy loses no correctness. Severity downgraded to medium because the false positive is real but has an easy workaround (phrase the result without invoke/run/start), so it degrades prose rather than being unavoidable.

### `check_trial.py:102` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Substring presence of 'acceptance' stands in for the judgment 'the triage handoff carries acceptance/success criteria'.
- **Code:** `if stage == "triage": ⏎     required_body.extend([snapshot, "acceptance"]) ⏎ ... ⏎ if needle.lower() not in body.lower(): ⏎     findings.append(f"handoff body missing {needle!r}")`
- **Concrete failure:** body = 'Definition of done: all whitespace-normalization tests green and no product diff.' carries acceptance criteria but lacks the literal word 'acceptance', so it is rejected with 'handoff body missing acceptance'.
- **Preserve:** Yes — requiring the triage handoff to carry acceptance criteria should stay; only the single-word substring proxy is the defect. (The 'snapshot' needle on this same line is a legit artifact-path reference check and must stay.)
- **Verify:** Confirmed at check_trial.py:101-102/105-107: for triage stage the body must contain the literal substring 'acceptance' (case-insensitive). This is a lexical proxy for the semantic judgment 'the handoff carries acceptance/success criteria.' The concrete failure holds and is reinforced by the skill's own template: handoff-packet.md line 82 labels it 'Acceptance criteria:' but line 70 uses 'definition of done' for the exact same concept — so a template-consistent body written as 'Definition of done: all tests green, no product diff' carries the criteria yet is rejected with \"handoff body missing 'acceptance'\". Genuine LEXICAL-FALSE-NEG / shape-policing.

### `check_trial.py:108` — LOW · SHAPE-POLICING
- **Proxy:** The regex requires the auto-advance declaration in a markdown table-cell shape ('Auto-advance | NO') rather than checking the meaning that auto-advance is disabled.
- **Code:** `if not re.search(r"Auto-advance\s*\|\s*NO\b", body, re.IGNORECASE): ⏎     findings.append("handoff must state Auto-advance NO")`
- **Concrete failure:** body = 'Auto-advance: NO (this handoff will not advance the pipeline).' clearly declares auto-advance disabled, but the required literal pipe separator is absent so it is rejected with 'handoff must state Auto-advance NO'. Same for prose like 'This handoff will NOT auto-advance to the next stage.'
- **Preserve:** Yes — the real invariant that the handoff must declare it does not auto-advance must stay; only the enforced '|' table-cell format is the defect.
- **Verify:** Confirmed at check_trial.py:108: `re.search(r"Auto-advance\s*\|\s*NO\b", body, ...)` requires a literal pipe, enforcing the markdown table-cell shape `| Auto-advance | NO |` rather than the meaning (auto-advance disabled). Concrete false-negative holds: body='Auto-advance: NO (this handoff will not advance the pipeline).' passes the line-106 'Auto-advance' substring check but fails line 108 because of the colon-vs-pipe, so a clearly valid declaration is rejected. Line 108 is also largely redundant with the line-105-107 substring check, whose only added effect is to police the pipe shape. This is genuine SHAPE-POLICING. Loosening the pipe to a proximity/`[:|]` check preserves the real invariant (must declare no auto-advance) without gutting any protected structural guard. Downgraded to low: narrow blast radius since it is an eval harness for a skill whose own template (handoff-packet.md:65) mandates exactly the pipe format, so a template-following agent is unlikely to hit it in practice.

### `check_trial.py:112` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Substring 'manual' (or 手动) stands in for the judgment 'the result communicates that the next stage must be triggered by hand'.
- **Code:** `if not re.search(r"manual|手动", output, re.IGNORECASE): ⏎     findings.append("user-visible result omits manual trigger")`
- **Concrete failure:** output = 'You must start the next agent yourself; nothing runs automatically.' communicates manual triggering but contains neither 'manual' nor '手动', so it is rejected with 'user-visible result omits manual trigger'. (The body needle 'manual' at line 100 has the same flaw.)
- **Preserve:** Yes — the requirement that the result communicate a manual trigger should stay; only the two-token substring proxy is the defect.
- **Verify:** Confirmed LEXICAL-FALSE-NEG at line 112. `re.search(r"manual|手动", output, re.IGNORECASE)` is a two-token substring proxy for the semantic judgment "result communicates the next stage must be triggered by hand." Concrete false-neg: output = "You must kick off wayne-verify yourself; nothing runs automatically." names the agent (passes line 114), avoids the invoke/run/start+agent regex (passes line 110), plainly communicates manual triggering, but contains neither 'manual' nor '手动' → wrongly rejected. Line 100's 'manual' body needle has the identical flaw. The semantic requirement should stay; only the substring proxy is the defect. The genuinely structural sibling — frontmatter schema check trigger:'manual' at lines 85/89-92 — is separate exact-value validation and is untouched by this finding.

### `check_trial.py:71` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requiring the machine route-slug 'escalate-incident' verbatim in user-visible output stands in for 'the verdict conveys an incident escalation'.
- **Code:** `if "NO_WAYNE_HANDOFF" not in output or "escalate-incident" not in output: ⏎     findings.append("external route must fail loud with NO_WAYNE_HANDOFF and verdict")`
- **Concrete failure:** output = 'NO_WAYNE_HANDOFF — this is a provider outage, escalated as an incident to on-call; no Wayne stage applies.' conveys the incident escalation in prose but omits the literal slug 'escalate-incident', so it is rejected.
- **Preserve:** Yes — the NO_WAYNE_HANDOFF sentinel is a legit fail-loud contract token and must stay; only forcing the human-readable output to echo the raw route slug 'escalate-incident' verbatim is the lexical-proxy defect.
- **Verify:** Confirmed at line 71. The `external` case has empty expected_agent, so no handoff packet/frontmatter exists and the route decision can only surface in `output`. Line 71 demands the verbatim machine slug `escalate-incident` in user-visible prose. This is a genuine lexical false-neg: every other case verifies the route slug in packet frontmatter (line 88) and never forces the raw slug into `output` — only external does, which marks it as an ad-hoc substring proxy for 'the verdict conveys an incident escalation.' The concrete failure is real: `NO_WAYNE_HANDOFF — provider outage, escalated as an incident to on-call` conveys the verdict and carries the sentinel but lacks the literal hyphenated slug, so it is wrongly rejected. The `NO_WAYNE_HANDOFF` sentinel half is legit fail-loud and must stay; only the `escalate-incident` verbatim clause is the defect, and the right fix is to accept semantic equivalents (e.g. 'incident'), not to remove the check.

## eval-skill-forge (5)

### `check_output.py:78` — MEDIUM · SHAPE-POLICING
- **Proxy:** A regex requiring every Observations bullet to BEGIN with exactly one [E#] followed by whitespace stands in for the real judgment 'is this observation grounded in cited evidence'.
- **Code:** `if not re.match(r"^- \[E\d+\]\s+\S", bullet): ⏎     findings.append(f"Observation bullet must start with one evidence ID: {bullet}")`
- **Concrete failure:** Bullet `- [E1], [E2] both show a null-pointer dereference` is rejected: after `[E1]` comes `,` not whitespace so `\s+` fails. Also `- The stack trace in [E3] proves the deref` is rejected because it opens with prose. Both are perfectly grounded observations citing real evidence IDs, but they don't match the single-ID-at-position-0 template.
- **Preserve:** The grounding invariant (bullets must reference evidence that exists in the source) is already fully enforced by the invented-IDs check at lines 65-69 (cited_ids - source_ids) and the 'at least one evidence bullet' check at 75-76 — those must stay. This line adds only a positional/shape constraint, no real invariant.
- **Verify:** Confirmed at check_output.py:78. The regex `^- \[E\d+\]\s+\S` forces every Observations bullet to begin with exactly one [E#] followed by whitespace — a template/shape proxy for the semantic goal "observation is grounded in cited evidence." Both concrete false-negatives genuinely hold: (1) `- [E1], [E2] both show a null-pointer dereference` fails because a comma, not whitespace, follows [E1] via \s+; (2) `- The stack trace in [E3] proves the deref` fails because it opens with prose. Both cite real, in-source evidence IDs and are perfectly grounded observations, yet are rejected purely for placement/count. This is SHAPE-POLICING that forces worse prose (lead with one ID, whitespace-delimited). The genuine grounding invariant is separately guarded: invented-ID detection at 65-69 (cited − source) and the at-least-one-bullet check at 75-76 must stay. This line is NOT one of the protected invariant classes (no hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, snapshot, event-order), and a correct fix — require each bullet to CONTAIN a grounded [E#] anywhere rather than START with exactly one — preserves per-bullet grounding while dropping only the positional constraint. Hence acting on the finding does not gut a real guard. Medium severity is appropriate.

### `check_output.py:47` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** `section in text` substring-scans the whole output for capitalized playbook section titles as a proxy for 'did the author actually emit a foreign playbook heading'.
- **Code:** `foreign = sorted(section for section in all_route_sections if section in text) ⏎ if foreign: ⏎     findings.append(f"NO_MATCH leaked playbook sections: {foreign}")`
- **Concrete failure:** For a NO_MATCH output whose single discriminator bullet asks `- Which file is the Source of truth: config.yaml or config.override.yaml?`, the ordinary phrase 'Source of truth' is a playbook section name and appears as a substring, so it is wrongly flagged as 'NO_MATCH leaked playbook sections: ['Source of truth']' even though no such heading was emitted.
- **Preserve:** The real invariant 'NO_MATCH must not emit playbook sections' is already enforced heading-level by `headings != ['Missing discriminator']` at line 45-46 — that structural check must stay. The substring scan is redundant with it and only adds false positives on prose.
- **Verify:** Confirmed LEXICAL-FALSE-POS at line 47. `foreign = sorted(section for section in all_route_sections if section in text)` does a case-sensitive substring scan of the entire output for capitalized playbook section titles. all_route_sections includes "Source of truth" (line 11, configuration-drift route). A legitimate NO_MATCH output with the bullet "- Which file is the Source of truth: config.yaml or config.override.yaml?" passes every structural check (first line, single ROUTE marker, no PLAYBOOK mention, headings == ['Missing discriminator'], one bullet, non-mutating) yet is wrongly failed at line 49 with "NO_MATCH leaked playbook sections: ['Source of truth']". "Source of truth" (SSoT) is a ubiquitous engineering phrase and is especially natural in a configuration-drift-adjacent NO_MATCH discriminator, making the collision plausible, not contrived. The finding's redundancy claim is also verified: the heading-level invariant is already enforced by `headings != ['Missing discriminator']` at line 45-46 (line 37 collects all '## ' headings), so any leaked playbook HEADING is already caught; line 47's substring scan only adds coverage for the phrase appearing in prose, which is exactly where the false positive fires.

### `check_output.py:68` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Same whole-text substring scan for other routes' section titles as a proxy for 'emitted a foreign heading', in the selected-playbook branch.
- **Code:** `foreign = sorted( ⏎     section ⏎     for section in all_route_sections - set(expected_sections) ⏎     if section in text ⏎ )`
- **Concrete failure:** A valid runtime-crash output with a bullet under '## Next probe' reading `- Establish a Baseline delta before re-running the crash repro` is flagged 'foreign playbook sections: ['Baseline delta']' because 'Baseline delta' (a performance-route section title) occurs as prose, even though the actual headings are exactly the three runtime-crash sections.
- **Preserve:** The correct heading-only check already exists: `headings != expected_sections` at line 63 verifies the emitted `## ` headings are exactly the selected route's sections. That must stay; this substring version is redundant and produces false positives on legitimate cross-references in bullet prose.
- **Verify:** Confirmed at check_output.py:65-69. The `foreign` check does a whole-text substring scan (`section in text`) for every other route's section title, in the selected-playbook branch. The actual heading invariant is already fully enforced at line 63 (`headings != expected_sections`), which captures every `## ` heading and requires exact match on content, order, and count — so any foreign title emitted AS A HEADING already fails there. The substring scan therefore only adds false positives on legitimate prose. Concrete false positive verified: for --expected runtime-crash, all_route_sections - expected includes 'Baseline delta'; a bullet under '## Next probe' reading '- Establish a Baseline delta before re-running the crash repro' sets `'Baseline delta' in text` True, yielding foreign=['Baseline delta'] and pass=False, even though the three emitted headings are exactly the runtime-crash sections. Several section titles are common phrases (notably 'Source of truth', 'Regression check', 'Profile target'), making the trigger easy to hit in valid cross-referencing prose. This is a textbook LEXICAL-FALSE-POS.

### `check_output.py:54` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A regex matching a mutating VERB at the start of the bullet (set|change|modify|update|restart|deploy|delete|write|enable|disable) is a proxy for the semantic judgment 'does this discriminator instruct a system mutation'.
- **Code:** `elif MUTATING_DISCRIMINATOR.search(bullets[0].strip()): ⏎     findings.append("NO_MATCH discriminator must not instruct a system mutation")`
- **Concrete failure:** False positive: the read-only question `- Change in error rate between region A and region B — which one fired the alert?` starts with 'Change' (a noun here) and is flagged as instructing a mutation. False negative (same proxy, opposite direction): `- Try setting the debug flag and observe the log` genuinely instructs a mutation but starts with 'Try', so the regex misses it and it passes.
- **Preserve:** The 'exactly one discriminator bullet' invariant at line 52-53 is legit and must stay. Whether a bullet is read-only vs mutating is a contextual reading a start-of-line verb regex cannot make; word-sense (change-the-noun vs change-the-verb) and non-leading verbs both break it.
- **Verify:** Confirmed at check_output.py:14-17,54-55. MUTATING_DISCRIMINATOR uses a start-of-line verb regex (set|change|modify|...) as a proxy for the semantic judgment "does this discriminator instruct a system mutation." Verified false-positive: `- Change in error rate between region A and region B — which one fired the alert?` matches `^-\s*change\b` (IGNORECASE) and is flagged, though "change" is a noun in a read-only diagnostic question. Verified false-negative: `- Try setting the debug flag and observe the log` does not match (leading token "Try" is not in the verb list) and passes, though it genuinely instructs a mutation. The proxy breaks on word-sense and on non-leading verbs — a LEXICAL-FALSE-POS/NEG. The adjacent structural check (exactly one bullet, lines 52-53) is legit and the finding preserves it; only the lexical part at line 54 is the defect.

### `check_output.py:50` — LOW · SHAPE-POLICING
- **Proxy:** A raw character count (>600) is used as a proxy for the semantic judgment 'is this DECLINE concise'.
- **Code:** `if len(text) > 600: ⏎     findings.append("DECLINE response is not concise")`
- **Concrete failure:** A DECLINE that concisely but completely states why review cannot proceed — e.g. one reason line naming the three specific pieces of missing evidence and why each blocks a causal verdict — can legitimately run ~650 chars and is rejected as 'not concise', even though it is exactly as long as it needs to be and no shorter.
- **Preserve:** The 'DECLINE must include a concise reason line' intent is already served structurally by the len(lines) < 2 check at line 48-49. The 600-char cap adds no real invariant; conciseness is a contextual judgment a byte count can't make.
- **Verify:** Confirmed at check_output.py:50 — `if len(text) > 600: findings.append("DECLINE response is not concise")`. A raw character count is a genuine lexical/shape proxy for the contextual judgment "is this concise," which a byte count cannot make: a 650-char DECLINE naming several distinct missing evidence pieces with per-item causal justification can be exactly as long as it needs to be, while a 400-char one can ramble. The finding's claim that the real intents are already structurally guarded elsewhere is accurate: "must not simulate the analysis" is enforced by the `if headings:` check (lines 46-47) and "must include a reason" by `len(lines) < 2` (lines 48-49). The 600-char cap adds no protected invariant (not hashing/E-row/schema/state-closure/coverage/snapshot/event-order), so removing it damages nothing structural — guts_real_invariant=false. Severity downgraded medium→low because 600 chars (~100 words) is a generous ceiling; the concrete false-negative is possible but atypical, so practical impact is modest. The `if headings:` and `len(lines) < 2` structural checks must be preserved.

## eval-plan (4)

### `check_trial.py:450` — MEDIUM · SHAPE-POLICING
- **Proxy:** Arrow-count >=2 stands in for the judgment 'does this scenario describe a real input/action/expected case', policing template shape instead of meaning.
- **Code:** `if scenario.count("→") < 2: ⏎     findings.append(f"U scenario lacks input → action → expected shape: {row_id}")`
- **Concrete failure:** A U row scenario cell 'delivery attempt exhausts retries → circuit breaker opens and rejects new sends' has exactly one arrow (count==1 < 2) and is rejected, even though it fully and clearly states cause and expected effect. Also 'toggle from OFF to ON → new route active' (one arrow) is rejected.
- **Preserve:** none — whether a scenario is meaningful cannot be proven by counting arrows; the surrounding row-shape checks (U\d id, owner-in-unit-set, US\d|added seed, surface regex at lines 440-449) are legit schema checks and must stay.
- **Verify:** Confirmed at check_trial.py:450 `if scenario.count("→") < 2`. This is a lexical SHAPE-POLICING proxy: arrow-count stands in for the semantic judgment "is this a real input/action/expected case." Concrete false-neg holds — a single-arrow scenario 'delivery attempt exhausts retries → circuit breaker opens and rejects new sends' has count==1 < 2 and is rejected despite fully stating cause and expected effect; likewise 'toggle from OFF to ON → new route active'. The check is separable from the surrounding legit schema guards (U\\d id/owner line 440, seed/status 446, surface regex 448, ownership/seed-disposition closure 452-488), which must stay. Downgraded from high to medium: `< 2` (unlike the canonical `!= 2`) already tolerates extra arrows and never passes bad plans — it only rejects valid single-arrow prose, so it is friction, not a correctness hole.

### `check_trial.py:53` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Substring ban on natural-language work phrases stands in for 'is this text a vague placeholder', flagging specific, actionable prose that happens to contain the phrase.
- **Code:** `BANNED = re.compile( ⏎     r"\b(?:TBD|TODO|implement later|add error handling|add validation|" ⏎     r"handle edge cases|write tests|similar to Unit)\b", re.IGNORECASE)`
- **Concrete failure:** Plan prose 'Unit 2 will add validation for the retry-count field per R3, and Unit 3 will write tests for circuit-breaker reopening' is a precise, actionable statement but is rejected because it contains 'add validation' and 'write tests'. Likewise 'add error handling for the socket-timeout branch in relay_queue' is banned though it names the exact case.
- **Preserve:** yes — the TBD / TODO / 'implement later' alternatives are genuine vague-placeholder bans and must be preserved; only the semantic work-phrase entries (add error handling, add validation, handle edge cases, write tests, similar to Unit) are the lexical proxy.
- **Verify:** Confirmed at check_trial.py:52-56 and applied at line 264 via BANNED.search(text) over the ENTIRE plan text. The alternation bans generic work-verbs (add validation, add error handling, handle edge cases, write tests, similar to Unit) bounded only by \b, so any occurrence anywhere fails the plan. Concrete false positives genuinely hold: 'Unit 2 will add validation for the retry-count field per R3' (specific, requirement-traced) is rejected on the substring 'add validation'; 'add error handling for the socket-timeout branch in relay_queue' (names the exact branch) is rejected; a legit cross-reference 'similar to Unit 3' is rejected. The regex cannot tell vague placeholder use from precise actionable prose — a pure lexical proxy for a semantic 'is this vague' judgment (LEXICAL-FALSE-POS / SHAPE-POLICING). It also pressures the skill-under-optimization to avoid natural phrasing. The TBD/TODO/implement-later alternatives are genuine placeholder markers and must be kept; only the semantic work-phrase entries are the defect, matching the finding's scoping.

### `check_trial.py:349` — MEDIUM · SHAPE-POLICING
- **Proxy:** Requires the literal token 'none — <reason>' (word 'none' + em-dash) as a proxy for 'a no-dependency unit justified itself', rejecting equally clear justifications in other wording.
- **Code:** `if not dependency_ids and not re.search(r"\bnone\s+—\s+\S", bodies["Dependencies"], re.IGNORECASE): ⏎     findings.append(f"{unit_id} Dependencies needs an earlier unit or exact 'none — <reason>'")`
- **Concrete failure:** Unit I1 Dependencies body 'None. This is the foundational unit; nothing precedes it.' clearly declares no deps with a reason but is rejected because it uses a period instead of the em-dash token and does not match the \bnone\s+—\s+\S regex. 'No dependencies (first unit)' fails identically.
- **Preserve:** yes — the invariant 'a unit with no dependencies must give a reason' should stay; only the exact 'none — ' token shape is the proxy and forces worse prose.
- **Verify:** Confirmed at line 349. dependency_ids (line 344) collects only \bI\d+\b tokens; a no-dep body like "None. This is the foundational unit; nothing precedes it." yields an empty set. The guard then demands regex \bnone\s+—\s+\S (literal 'none' + em-dash), which a period-punctuated "None." or "No dependencies (first unit)" fails, so a semantically clear justification is rejected purely on token shape — a real LEXICAL/SHAPE-POLICING false-neg. Empty fields are already caught separately at lines 342-343, so 349 only polices phrasing, not content. It is distinct from the genuine DAG/state-closure guard at line 347 (dependency_ids.issubset(known_earlier)), which must stay; relaxing the em-dash token does not touch that structural check.

### `check_trial.py:456` — LOW · LEXICAL-FALSE-POS
- **Proxy:** Raw substring containment stands in for 'does this unit own this surface', so a surface that is a textual prefix of a different owned surface passes ownership wrongly.
- **Code:** `if surface not in bodies.get("Files", "") and surface not in bodies.get("Produces", ""): ⏎     findings.append(f"{row_id} surface is not owned by {owner} Files or Produces: {surface}")`
- **Concrete failure:** If the unit's Produces lists 'relay_queue/queue.py::added' and a U row surface is 'relay_queue/queue.py::add', the substring 'relay_queue/queue.py::add' is found inside '...::added', so the ownership check passes even though 'add' is not actually a produced/owned symbol.
- **Preserve:** yes — the ownership-linkage invariant (a U surface must belong to its owner's Files/Produces) is legit and must stay; only the substring test should be a token/line match instead of raw `in`.
- **Verify:** Confirmed at line 456: U-row surface ownership is decided by raw substring `in` against the Files/Produces text. Surfaces are structured path::symbol strings, so a U surface that is a substring of a genuinely-owned superstring passes wrongly — e.g. U surface 'relay_queue/queue.py::add' is `in` Produces text 'relay_queue/queue.py::added', so the ownership check silently passes for a symbol ('add') that is never actually produced. Verified this is the ONLY gate on U-surface ownership: the exact `produced` set is used only for the Consumes check (line 368), and line 484 only checks U-ID linkage, not surface strings — so the hole is genuinely reachable. It is a real lexical proxy standing in for a structural ownership judgment. Note: the finding's LEXICAL-FALSE-POS label is technically off — this is a validator false-negative (too lenient / wrongly passes), not a flagging of innocent text — but it still qualifies as a proxy causing a false neg. Severity low: it requires a prefix/substring coincidence (add/added, run/runner, path substrings) rather than firing generally.

## eval-visual-synthesis (4)

### `check_trial.py:177` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A negation-blind proximity regex stands in for the judgment 'did the agent actually skip Level 2', firing on any 'Level 2' within 40 chars of 'skipped/not run' regardless of polarity.
- **Code:** `if re.search(r"(?i)level\s*2.{0,40}(?:not run|skipped)|(?:not run|skipped).{0,40}level\s*2", text, re.DOTALL): ⏎     findings.append("byte-identical case skipped required Level 2")`
- **Concrete failure:** Compare output states 'Level 2 was not skipped; I ran all ten facets.' -> 'Level 2' + ' was not ' (9 chars) + 'skipped' is within the 40-char window, so the check reports 'byte-identical case skipped required Level 2' even though the agent explicitly confirmed it did NOT skip it.
- **Preserve:** Yes -- the byte-identical compare case must not skip the required Level 2 ledger comparison. Keep an anti-skip check, but it must read the actual claim (e.g. an explicit 'Level 2: not run' status field), not a polarity-blind substring proximity.
- **Verify:** Confirmed at check_trial.py:177. The regex `(?i)level\s*2.{0,40}(?:not run|skipped)|(?:not run|skipped).{0,40}level\s*2` is a negation-blind proximity match: it flags a failure whenever "level 2" is within 40 chars of "skipped"/"not run" in either order, ignoring polarity. Concrete false-positive holds: "Level 2 was not skipped; I ran all ten facets." matches (" was not " = 9 chars between "Level 2" and "skipped"), wrongly appending "byte-identical case skipped required Level 2" for an output that explicitly confirms Level 2 ran. "no facets skipped in Level 2" also fires via the second alternation. This is a MISCLASSIFIED-ORACLE/LEXICAL-FALSE-POS: a substring proximity standing in for the semantic judgment "did the agent actually skip Level 2". guts_real_invariant=false: the legit requirement that Level 2 be present/produce a ledger result is already independently enforced by check_two_levels (line 166) and require_any("ledger equality") (lines 179-194); line 177 is only the anti-skip lexical proxy and touches none of the protected structural guards (hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, exact-snapshot, event-order). Replacing it with an explicit-status read preserves the anti-skip intent without lexical false positives. Severity downgraded high->medium because the most common correct phrasing avoids the trigger words, so real-world false-positive frequency is moderate.

### `check_trial.py:146` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Requires the jargon word 'targetable' as a proxy for 'the ledger produced targetable structures', policing that the author uses the skill's terminology rather than whether elements are actually addressable.
- **Code:** `require_any(text, findings, "targetable structures", ("targetable", "可定位结构", "目标结构"))`
- **Concrete failure:** Describe output lists every element with a stable ref id and pixel geometry (fully addressable/targetable in substance) but describes them as 'individually addressable elements with coordinates' -> flagged 'missing targetable structures' purely because the literal word 'targetable' is absent.
- **Preserve:** No -- addressability is proven by the presence of refs/geometry the ledger already requires (coverage/elements checks stay); this line only enforces a jargon token and can be relaxed without losing any structural guarantee.
- **Verify:** Line 146 require_any only accepts the jargon token "targetable" (plus two Chinese jargon phrases "可定位结构"/"目标结构") with no natural-language alternative, unlike neighboring checks (e.g. line 145 accepts plain "contains"/"containment"). Since require_any (lines 42-45) is a pure casefold substring match, an otherwise-complete describe output that proves addressability naturally ("each element individually addressable via stable ref id and bounding-box coordinates") passes every other check yet is flagged "missing targetable structures" solely because the literal word is absent. That is a genuine LEXICAL-FALSE-NEG / shape-policing on the skill's terminology rather than on whether elements are actually addressable. It does NOT guard any structural invariant: addressability is not otherwise verified by ref/geometry checks, and this line only tests for a word, not for the property. Removing/relaxing it (adding natural synonyms) loses no hashing, schema, coverage, snapshot, or state-closure guarantee. Medium (not high) because it lives in a controlled eval where the tested agent typically adopts the skill's vocabulary, limiting trigger frequency.

### `check_trial.py:72` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Detects the synthesis section by a fixed list of heading synonyms; a valid synthesis under any unlisted heading title is treated as absent, and the ledger-before-synthesis ordering check silently no-ops.
- **Code:** `synthesis = first_heading(text, ("synthesis", "综合解读", "综合描述", "综合分类", "综合结论", "核心结论", "总结", "结论")) ⏎ ... ⏎ if synthesis < 0: ⏎     findings.append("missing synthesis derived from the ledger")`
- **Concrete failure:** Agent titles its post-ledger interpretation section '## Interpretation' (or '## What this dashboard shows'). None of the listed synonyms match -> first_heading returns -1 -> reported 'missing synthesis derived from the ledger', and the ledger>synthesis ordering guard at line 80 cannot run.
- **Preserve:** Yes -- the ledger-must-precede-synthesis ordering (line 80) is a genuine methodology invariant and must stay. Only the lexical heading-title detection is the defect; the ordering check should key off a more robust synthesis-section signal.
- **Verify:** Confirmed at check_trial.py:70-81. `check_ledger_first` detects the synthesis section only by substring-matching a heading against the fixed list ("synthesis","综合解读","综合描述","综合分类","综合结论","核心结论","总结","结论"). The SKILL.md contract (Process D) says only that "the first user-visible conclusion or summary must appear after the VEL" — it never mandates a heading literally named "Synthesis." The English synonym set is just the single word "synthesis"; English "summary" and "conclusion" are missing even though their Chinese equivalents 总结/结论 are present. So a faithful agent titling its post-ledger section "## Summary", "## Conclusion", "## Interpretation", or "## What this dashboard shows" yields first_heading=-1 → falsely reports "missing synthesis derived from the ledger", and the line-80 ordering guard no-ops because synthesis<0. That is a real LEXICAL-FALSE-NEG policing heading shape rather than meaning. The presence-of-a-conclusion and the ledger-before-synthesis ordering are legit invariants, but they are damaged BY the lexical detection, not protected by it — the fix is to key the synthesis signal off a more robust content marker while keeping the ordering check.

### `check_trial.py:143` — LOW · LEXICAL-FALSE-NEG
- **Proxy:** Requires the literal token 'x axis' as a proxy for 'the agent captured the chart's axis structure', policing vocabulary rather than whether the axis was actually described.
- **Code:** `require_any(text, findings, "chart contract", ("x_axis", "x axis", "x-axis", "x 轴", "x轴"))`
- **Concrete failure:** Describe output for the Fleet Console chart says 'Queue depth runs along the horizontal axis; the vertical axis lists the two edge nodes.' -- a complete, correct axis description that never uses the token 'x axis'/'x_axis' -> reported 'missing chart contract'.
- **Preserve:** No -- it only asserts a vocabulary token is present; the real goal (axis structure captured) is satisfied by 'horizontal axis'. No structural invariant is lost by relaxing the token set.
- **Verify:** Line 143 is a require_any keyword-presence check on the agent's describe trial output, accepting only x_axis/x axis/x-axis and two Chinese variants as a proxy for 'the chart's axis structure was captured.' A complete, correct description of the fixture chart's axes using standard synonyms (e.g. 'the horizontal axis lists Mon-Thu; the vertical axis is queue depth 10-30') carries the exact axis meaning yet contains no x-token, so it is wrongly reported as 'missing chart contract.' That is a genuine LEXICAL-FALSE-NEG that polices vocabulary rather than the semantic goal. It guards no structural invariant (no hashing, E-row carry, decision-coverage, schema, state-closure, snapshot, or event-order); it is pure keyword presence on freeform prose, so broadening the token set to include 'horizontal/vertical axis' loses nothing. Severity downgraded to low because the skill's own carrier-contracts.md mandates a literal x_axis field and structured tables are the default output form, so a compliant agent very likely emits the token anyway, keeping the real-world false-neg incidence small; the narrow gap is mainly prose-style axis descriptions.

## eval-verify (3)

### `check_trial.py:283` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** Mere mention of the words BLOCKED/FAILED/wayne-work is treated as the agent having blocked the gate, regardless of the sentence's actual meaning.
- **Code:** `if re.search(r"BLOCKED|FAILED|wayne-work", output, re.IGNORECASE): ⏎     findings.append("legitimate skip incorrectly blocked the gate")`
- **Concrete failure:** A correct legit-skip acceptance: "No runtime surface here — this is a legitimate skip, not a FAILED verification, so no wayne-work is needed." passes the acceptance check at line 281 (contains 'legitimate') yet also matches FAILED and wayne-work here, producing the contradictory false finding 'legitimate skip incorrectly blocked the gate'. The author is forced to avoid naming the very concepts it is explaining.
- **Preserve:** Yes — 'a legit skip must not be blocked' is real, but must be judged from an actual block verdict (routed to wayne-work / status ❌), not from the presence of these words in explanatory prose.
- **Verify:** Confirmed at check_trial.py:283. The legit-skip case accepts on 'legitimate' (line 281) then re-fails on any occurrence of BLOCKED|FAILED|wayne-work (line 283, case-insensitive). The concrete input 'No runtime surface here — this is a legitimate skip, not a FAILED verification, so no wayne-work is needed.' passes 281 (matches 'legitimate') yet trips 283 on 'FAILED' and 'wayne-work', yielding the contradictory finding 'legitimate skip incorrectly blocked the gate'. This is a genuine LEXICAL-FALSE-POS: word presence proxies for the semantic 'did the agent actually block/route away' judgment. Proof it is meaning-dependent, not shape: sibling cases stale-green(237), startup-failure(246), multi-row(276) REQUIRE the identical tokens as evidence of CORRECT routing — same words, opposite verdict, distinguished only by sentence meaning. The regex cannot read that. The natural way to explicitly accept a legit skip is to contrast it against failure, which is precisely what trips the check.

### `check_trial.py:237` — MEDIUM · MISCLASSIFIED-ORACLE
- **Proxy:** The semantic judgment 'agent routed away from shipping' is decided by matching a fixed grab-bag of keywords, so clear routing-away prose without those exact tokens is rejected.
- **Code:** `if not re.search(r"FAILED|not ready|不能 ship|不可.*ship|wayne-work", output, re.IGNORECASE): ⏎     findings.append("observable failure did not route away from ship")`
- **Concrete failure:** stale-green agent clearly routes away: "Runtime surfaced a regression; do not ship this — hand it back for a fix." It contains none of FAILED / 'not ready' / '不能 ship' / '不可.*ship' / wayne-work (it says 'do not ship', and '不可.*ship' needs the literal Chinese 不可), so the check falsely reports 'observable failure did not route away from ship'. Same lexical-routing proxy at lines 246, 251, 258, 276, 281.
- **Preserve:** Yes — that a failed run routes away from ship is real behavior, but it is already carried by the deterministic ❌ row-status check and by reports_passed()==False; the keyword list adds a vocabulary tax on top.
- **Verify:** Verified at check_trial.py:237. The check decides the semantic judgment "agent routed away from ship" via a fixed keyword grab-bag (FAILED|not ready|不能 ship|不可.*ship|wayne-work, IGNORECASE). The concrete failure holds: "Runtime surfaced a regression; do not ship this — hand it back for a fix." clearly routes away but matches none of the tokens — notably the regex has Chinese don't-ship phrases (不能/不可 ship) but NO English "do not ship"/"don't ship"/"blocked"/"reject", an asymmetric lexical gap. This is a genuine MISCLASSIFIED-ORACLE / LEXICAL-FALSE-NEG that would fail a correctly-routing agent for phrasing. It recurs at lines 246, 276 (identical regex) and analogously 251/258/281. guts_real_invariant=false: the real structural guards for this case are on separate lines — row_status(repo)!="❌" (233) and reports_passed(output) (235) — and line 237 is a pure lexical add-on. Broadening or removing the keyword check does not touch hashing, E-row carry, decision-coverage, schema, state-closure, file-mutation, snapshot, or event-order; the correct fix is to widen the regex to include English routing phrases, not to gut the deterministic ❌/ship-gate checks. Severity medium is appropriate: this is an eval harness, so the impact is false-failing legitimate skill trials (eval fidelity) rather than a wrong production ship, but the gap is systemic across multiple cases.

### `check_trial.py:170` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** The 'reports passed' oracle negates the Chinese branch only for the single character 未 immediately preceding 准备好, so most real negations of 'ready to ship' still count as PASSED.
- **Code:** `r"RUNTIME VERIFICATION:\s*PASSED|(?<!未)准备好 ship"`
- **Concrete failure:** Agent output "当前行为已损坏，还没准备好 ship，请回到 wayne-work" ('not yet ready to ship'): the char before 准备好 is 没, not 未, so the lookbehind passes and reports_passed() returns True. In stale-green/startup-failure/multi-row this fires 'incorrectly passed the ship gate' (lines 236/245/275) against a correct agent that actually said it is NOT ready to ship.
- **Preserve:** Yes — detecting the PASS assertion is real; the English 'RUNTIME VERIFICATION: PASSED' marker is a legit mandated token and must stay. Only the Chinese branch's one-character negation guard is broken (any negator like 没/尚未/不 should suppress it).
- **Verify:** Confirmed by reading check_trial.py:167-174 and running the regex. `(?<!未)准备好 ship` only excludes the single char 未 directly before 准备好. Input "还没准备好 ship" ("still not yet ready to ship") uses 没, not 未, so the lookbehind passes and reports_passed() returns True — a wrong PASS detection on a correct NOT-ready assertion. In stale-green (236), startup-failure (245), and multi-row (275), this makes a correct agent get flagged "incorrectly passed the ship gate". Other negators (不能/没) also leak through; only 尚未/未 endings are caught. This is a lexical/mechanism proxy standing in for the semantic judgment "did the agent claim success", so it fits LEXICAL-FALSE-POS / MISCLASSIFIED-ORACLE. The English 'RUNTIME VERIFICATION: PASSED' token is legit and can stay; only the one-char Chinese negation guard is broken.

## validate_goal_prompt (2)

### `validate_goal_prompt.py:51` — MEDIUM · LEXICAL-FALSE-POS
- **Proxy:** A whole-document substring scan stands in for the judgment 'the verification/completion wording is vague', flagging the banned phrases wherever they appear regardless of semantic role.
- **Code:** `if re.search(r"(?i)\brun (?:the )?tests\b|works well|looks good|更好看", text): ⏎     findings.append("goal contains vague verification or completion language")`
- **Concrete failure:** A goal whose Context section factually describes the current state — e.g. 'Context:\nThe dashboard looks good on desktop but the mobile layout is broken; CI also cannot run the tests because the runner image lacks CUDA.' — is rejected as 'vague verification or completion language', even though 'looks good' and 'run the tests' are here describing the problem to fix, not the verification. The regex matches over the entire text, not only the Verification/Completion sections.
- **Preserve:** Partially — it aims to keep verification/completion criteria concrete rather than hand-wavy. That intent is legit, but banning substrings anywhere in the document cannot prove it; if kept it should at minimum be scoped to the Verification/Completion sections, and even then substring bans over-reach. The structural section checks are unrelated and must stay.
- **Verify:** Confirmed: line 51 runs re.search over the entire `text`, not scoped to any section (unlike the neighboring line 48-49 backtick check, which correctly slices text[find(V):find(C)]). The banned phrases 'run (the) tests', 'works well', 'looks good', '更好看' therefore fire wherever they appear. The concrete input — a Context section reading 'The dashboard looks good on desktop but the mobile layout is broken; CI also cannot run the tests because the runner image lacks CUDA' — genuinely matches both 'looks good' and 'run the tests' and is rejected as 'vague verification or completion language', even though these phrases describe the problem, not the verification. It is a lexical substring proxy for a semantic 'is this criterion concrete?' judgment. It guards no protected invariant class; scoping or removing it leaves the independent section-count/order/backtick structural checks intact.

### `validate_goal_prompt.py:49` — MEDIUM · LEXICAL-FALSE-NEG
- **Proxy:** Presence of a backtick-delimited span is used as a proxy for 'the verification step is exact/executable', policing markdown formatting rather than the concreteness it is meant to carry.
- **Code:** `verification = text[text.find(REQUIRED[3]) : text.find(REQUIRED[4])] ⏎ if not re.search(r"`[^`\n]+`", verification): ⏎     findings.append("verification section has no exact backticked command")`
- **Concrete failure:** A fully concrete verification section written without backticks is rejected — e.g. 'Verification required before completion:\nRun pytest tests/test_auth.py and confirm all cases pass, then open http://localhost:8765/dashboard and confirm the new Status column renders.' This names an exact command, an exact path, and an exact URL, yet fails because nothing is wrapped in backticks. It also forces authors to add backticks purely to satisfy the linter.
- **Preserve:** The intent (verification must be concrete/executable) is a real invariant, but the backtick check is a pure lexical proxy for it. The already-present heading checks that confirm the Verification section exists and is unique are the legit structural part and must stay; only the backtick-presence requirement is the proxy.
- **Verify:** Confirmed at validate_goal_prompt.py:48-50. The check `re.search(r"`[^`\n]+`", verification)` requires a backtick-delimited span as a proxy for 'verification is exact/executable'. This is a genuine LEXICAL-FALSE-NEG/shape-policing: a fully concrete verification without backticks (e.g. 'Run pytest tests/test_auth.py and confirm all cases pass, then open http://localhost:8765/dashboard and confirm the new Status column renders.') names an exact command, path, and URL but is rejected purely for lacking backticks. Conversely a trivial `x` backtick span passes without being a command. So the check polices markdown formatting, not concreteness, and forces authors to add backticks to satisfy the linter. The concreteness intent is real, but it is also partially covered by the separate vague-language check at line 51; the backtick presence requirement itself is a pure proxy.

## DO NOT GUT — real invariant with a lexical smell

- **`check_trial.py:82`** (eval-test-design) — proof_axes() decides which semantic proof axis a row asserts (a contextual reader's judgment) purely by keyword vocabulary, so equivalent prose is not classified.
  - Why keep: Verified at check_trial.py:82-88. The stream-axis classifier is purely lexical: the prose "response is emitted incrementally as it is generated" (a legitimate streaming Observable) matches none of the four alternatives — no "token(s)", and "response" is absent from the (output|answer|reply) alternation — so proof_axes() returns no 'stream' axis and line 160 wrongly reports "missing independent proof axis: stream". The concrete false-negative genuinely holds; classifying whether a row asserts streaming is an equivalence judgment only a contextual reader can make, so this is a real MISCLASSIFIED-ORACLE lexical proxy. BUT guts_real_invariant is true: proof_axes() is the load-bearing classifier for the proof-axis DECISION-COVERAGE check (lines 158-164 require all four axes present, each in its own row, none collapsed). There is no deterministic semantic classifier; any action that loosens proof_axes() to accept arbitrary prose would let rows that do not actually prove streaming pass the coverage gate, destroying the coverage invariant's correctness. Severity corrected to medium rather than high: impact is confined to an eval harness where the tested skill can be steered toward expected vocabulary, and the classifier already carries several synonyms, so the false-fail is real but bounded and not a production correctness hole.
- **`check_candidate_static.py:48`** (eval-test-design) — Skill intent is verified by exact DOT node ids and verbatim label text, a lexical proxy for 'the flowchart has an unresolved-conflict blocked terminal'.
  - Why keep: Confirmed at check_candidate_static.py:48-51. The check proves skill intent ("unresolved-conflict routes to a blocked terminal") by matching a verbatim DOT label ('Write blocked matrix; no plan handoff') plus a hardcoded node-id edge ('I -> K [label="yes"]'). This is a lexical/shape proxy for a structural route, not a semantic check: a semantically-identical flowchart that renames node K or rewords the label (e.g. 'Write blocked matrix (no plan handoff)') still encodes the blocked terminal but reports 'missing intent'. The false-negative is proven LIVE on the sibling checks the finding groups in (lines 52-60): the canonical production SKILL.md drifted node J's label from the required 'Invoked by mind-explode?' to 'Nested design caller?', and running the check against the real skill emits 'FAIL: missing intent: mind-explode return-only route' and 'FAIL: missing intent: standalone plan handoff route' even though those routes are still present in the DAG. So the verbatim-prose matching genuinely rejects valid content. Severity is low: impact is friction/false-negatives inside the internal skill A/B eval harness, not runtime skill correctness.
- **`check_trial.py:202`** (eval-verify) — An exact command substring is used to prove 'the real entrypoint ran', so a semantically-equivalent invocation is rejected even when the run's real artifact is present.
  - Why keep: Confirmed at check_trial.py:202 (and the parallel server matcher at 216-217). `require` is a bare `needle not in text` (line 163) over real agent trace commands (lines 71-97), so the exact-substring proxy genuinely false-negatives on semantically identical invocations: quoted args, `./data/input.txt`, or — most realistically — curl flag reordering (`curl -sS -f` vs the required `curl -fsS` at line 217). That lexical brittleness is real. HOWEVER the finding's remediation rationale is wrong: it claims the require is REDUNDANT because 204-210 already proves the entrypoint ran. It does not. The artifact check (line 204: result.txt == 'ALPHA') is trivially spoofable by `echo ALPHA > output/result.txt`; combined with editing the ✅ row and printing 'RUNTIME VERIFICATION: PASSED', a fabricating agent passes cli-success with the require removed. The require is the anti-fabrication / proof-of-execution guard, distinct from the content check. So the defect is a brittle matcher, not a redundant one — the correct fix is to normalize/tokenize the command match (order-independent flags, quote-insensitive args), NOT to delete the check.
