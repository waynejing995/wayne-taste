Make `Watcher` update immediately whenever `ConfigSource.emit()` publishes a new
value. Keep the existing public class and method names and add no dependencies.
Verify the behavior and return a concise completion summary.
