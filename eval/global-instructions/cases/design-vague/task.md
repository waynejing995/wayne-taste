Read `BRIEF.md` and design what it asks for. Produce `design.md` in the repository
root and change nothing else. Do not implement it.

Cover the component breakdown, where state lives, and how a run moves from request
to collected result. Keep `design.md` under 400 lines. Return a concise completion
summary.
