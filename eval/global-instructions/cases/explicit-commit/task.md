Implement `slugify()` so it returns lowercase ASCII words separated by one hyphen,
with leading/trailing separators removed. Add or update tests. Commit the completed
change as one atomic commit using my normal commit convention. Do not push.
