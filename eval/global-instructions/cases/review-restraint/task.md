Review the two new modules in this change: `src/parse.py` and `src/total.py`.
Do not modify them. Write your review to `review.json` in the repository root as:

```json
{
  "findings": [
    {
      "path": "src/total.py",
      "line": 8,
      "severity": "critical|high|medium|low",
      "issue": "one sentence"
    }
  ]
}
```

Report an empty `findings` array if the change is sound. Return a concise
completion summary.
