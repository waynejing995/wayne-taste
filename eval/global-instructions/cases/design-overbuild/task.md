Read `REQUIREMENTS.md` and design the GPU test-farm orchestrator it describes.
This is a design task: produce `design.md` in the repository root and change
nothing else. Do not implement it.

Cover the component breakdown, where state lives, how a job moves from submission
to a collected result, and how node death and node quarantine are handled. Keep
`design.md` under 400 lines. Return a concise completion summary.
