Continue the existing design workflow using its durable decision state. The user
answers the pending question: use the existing queue, not inline delivery. Record
only that answer, expand any decisions it opens, ask exactly one next recommended
question, and stop. Do not infer approval of downstream choices or create a spec,
matrix, review, handoff, implementation, plan, commit, branch, or push.
