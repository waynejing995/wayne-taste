# Locked decisions: queued webhook delivery

The durable decision log has a locked, fully resolved decision frontier. Locking
the decisions freezes design inputs; it is not implementation authorization. The
written design sections have not been approved. Continue from the next valid
design-stage gate without implementing or planning.
