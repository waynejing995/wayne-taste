Read `/workspace/skill/SKILL.md` completely and follow it. Use only that supplied
design skill. Work in `/workspace/repo`; read its instructions, case, source, docs,
and durable decision log.

The user answers pending node N1: use the existing queue, not inline delivery.
Record only that answer, expand every causal child it opens, ask exactly one next
recommended question, and stop. Do not approve a child, converge, write downstream
artifacts, implement, plan, commit, or invoke another stage. Return only the concise
user-visible response.
