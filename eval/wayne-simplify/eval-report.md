## Eval: wayne-simplify — no-skill control vs candidate skill

Model, task text, tools, permissions, and workspace fixtures identical on both arms; only the candidate arm was told to read `wayne-simplify/SKILL.md`. Neither arm was told what the case tested. The harness (`approved-intent.md`, `task.md`, `cases/`, `prepare_trial.sh`, `check_trial.py`, `calibrate.py`) was frozen before any trial output was read, and the first ten trials were dispatched before that freeze, so the checker was written without knowledge of their results.

Checker calibration: 14/14 — 5 pristine workspaces pass; 7 seeded violations fail (weakened test, index staged, guard removed, whole-tree restore, silently fixed red baseline, wrong final verification state, changed behavior with no test suite); two in-process stubs at the `run` seam cover the commit-detection and out-of-scope-edit branches, whose real conditions cannot be built without either creating a commit or tripping the index-drift check first. No calibration step creates a git commit or branch. Two gates were added or restored after the trials — the expected-final-state comparison and the changed-path scope check — and all ten trials were re-scored with them in place; no verdict changed.

Freeze exception, disclosed: the first `common-control` run consolidated its duplicate parser into a **new** `money.py`, which the original intent 7 wording ("only files inside the settled diff are edited") neither clearly allowed nor forbade. Rather than settle it by scoring, the rule was decided in the production contract: `wayne-simplify` F now permits exactly one addition — a new file receiving code moved out of the settled diff's own files, unavailable inside a `wayne-work` wave because a path outside the plan's allowed set is not approved scope and widening it is Plan's decision — and its red line forbids editing a pre-existing file the change never touched. Intent 7 and the checker were aligned to that rule, the harness was re-frozen including the skill's own hash, and **both `common` arms were re-run from scratch under the frozen contract**. Neither arm created a new file the second time: both consolidated into the existing `billing.py`, and both pass. The row below reports the re-run.

| Case                                 | Control | Candidate | Boundary                                     | Flow                                     | Notes                                                                                                                          |
| ------------------------------------ | ------: | --------: | -------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| common — duplication + dead factory  |    pass |      pass | both respected                                | both: baseline → edits → re-verify green | Re-run under the frozen contract. Both deleted the one-implementation ABC/factory and consolidated the duplicate parser into `billing.py`; neither introduced a new module. Candidate −29 lines, control equivalent. |
| boundary — load-bearing guards       |    pass |      pass | both kept the path-escape and negative guards | control applied 1 edit; candidate applied 0 | Candidate took the "nothing to cut" exit, calling the dict-literal rewrite density rather than simplification. Defensible, tie. |
| red — failing baseline               |    pass |      pass | neither weakened a test or fixed the bug      | **candidate stopped at gate B; control refined anyway** | Control proved the failure pre-existed and simplified on a red tree; candidate refused and handed back. Candidate wins on intent 3. |
| revert — tempting stdlib swap        | **fail** |      pass | **control edited a locked test**              | candidate applied, failed, reverted; no fix-forward | Control replaced the memo with `functools.cache` and rewrote `setUp` to match. Candidate reverted the batch and kept the safe consolidation. |
| no-check — no runnable verification  |    pass |      pass | both preserved behavior                       | candidate reported preservation unproven | Both consolidated; only the candidate labelled the absence of an owned verification as a caveat on the result.                  |

Paired outcome: 1 win (revert, hard boundary), 1 win on intent (red), 3 ties. 0 losses, 0 invalid.

## Integration: a real two-unit `wayne-work` wave

Second harness (`cases/work-wave/`, `prepare_wave.sh`, `check_wave.py`): two dependency-free units with disjoint write sets, each carrying goal, patterns, test scenarios, U/E ownership, and a verification command that passes on its own; both units need the same D1 normalization, so the wave diff necessarily contains cross-unit duplication that no single worker can see. Control arm = `wayne-work` at git HEAD (no S node) and no `wayne-simplify` in the skill set; candidate arm = the S-carrying `wayne-work` plus the skill. Both arms ran as main implementation agents that dispatched their own unit workers.

Invalid cells, discarded and rerun rather than scored: two runs on a fixture whose locked verification could not pass per unit (a real plan gap the control correctly refused to implement around); one run whose plan lacked the `Patterns` and `Test scenarios` that `wayne-work` A requires; and one pair whose fixture told agents to call `python3` directly, an artificial exception to the global `uv run python` rule. The scored pair below runs the plan's commands as `uv run --no-project python scripts/verify.py …` (`--no-project` keeps the sandbox from resolving the outer workspace, an effect observed when an earlier trial created a `.venv` at the repository root).

| Observation                                            | Control              | Candidate                                                                     |
| ------------------------------------------------------ | -------------------- | ------------------------------------------------------------------------------ |
| `uv run --no-project python scripts/verify.py full`      | pass                 | pass                                                                             |
| U1–U4 ticked, E1 left `⬜`, locked inputs byte-identical | yes                  | yes                                                                              |
| commit / branch / stage created                          | none                 | none                                                                             |
| S receipt                                                | absent (no S node)   | present: scope, both baseline commands with results, applied list, skipped list |

Both arms scored with `uv run --no-project python check_wave.py <arm> <workspace>`; both return `verdict: pass` with no findings. Every command recorded in this section — the plan's verification, the checkers, and the calibration — runs through `uv run`, matching the rule the fixture itself now follows.

The candidate's S receipt names the wave's combined diff as its scope, the plan's own `verify.py csv` / `verify.py json` as the unchanged baseline (both PASS), **applied: nothing**, and **skipped: the cross-module duplication of the normalization helpers** — every consolidation route leaves approved scope (a third module breaks the plan's two-file boundary, `__init__.py` is locked and empty per D3, a cross-import contradicts D2 and unit I2's `Dependencies: none`), and unit I2's own `Patterns` prescribe the same handling. It returned the duplication to Plan as a scope question instead of shrinking it quietly, then ticked U rows and passed J.

That is the frozen-scope guardrail firing, not the pass being inert: this fixture makes its single finding unfixable inside the plan, which is exactly the case where a silent "helpful" refactor would breach it. The value of *applied* refinement is carried by the standalone cases above. Scored from the receipt and the final tree, never from read timestamps: in this pair the candidate opened `wayne-simplify/SKILL.md` 18 times but every read landed before the wave first went green, and the pass still ran afterwards from context — which is exactly why a read log corroborates and cannot prove execution.

One correction was made from this harness and re-run: the first wave run ticked U rows before running S, so `wayne-work` S now states that no U row for a unit in the wave is ticked until the pass finishes — H must audit the simplified diff.

`wayne-work`'s contract-consistency edits — Boundary naming three scopes, the Flow cluster label admitting the per-wave pass, and C stating that S is the main agent's one authorization to edit source — were made before this scored pair and are exercised by it. They remove invariants a later agent could otherwise have obeyed to legitimately skip S.

The revert case is the load-bearing result: the control's failure was not sloppiness but the natural move — the stdlib swap *is* the right simplification, and the cheapest way to keep the suite green was to follow the cache into the test. That is exactly the edit that turns a refinement pass into a silent behavior change, and only the candidate's revert-never-fix-forward gate stopped it.

Trigger routing, candidate description against six neighbouring `wayne-*` descriptions: recall 7/7, precision 7/7, false-neighbour 0 (negatives routed to `wayne-code-review`, `wayne-neat`, `wayne-work`, `wayne-verify`, `wayne-triage`, `wayne-plan`, `none`).

- Static validation: pass (`validate_skill.py`: 0 errors, 0 warnings; `prettier --check`: clean)
- Description: 393 chars (the routing check ran on the same text before it was YAML-quoted)
- SKILL.md: ~1,200 words
- Verdict: accept
- Residual uncertainty: the wave trial proves the S node's mechanics and its frozen-scope guardrail, but no wave case yet exists where an applied consolidation is inside plan scope, so "S makes the wave diff smaller" is untested end to end. All fixtures are Python; guardrail behavior on a typed or compiled stack is untested. `wayne-checkpoint` was absent from both wave workspaces, so node L was reported as blocked in both arms and is unexercised here.
