# Repeated controller failure

Triage the retry-controller failure recorded in `artifacts/fix-history.md`. The
artifact is the complete local evidence; do not fetch anything. Each attempted
fix moved the break to another controller boundary, so another patch is not an
approved response. Diagnose and route without editing product files.

Handoff approval: granted for one return-only internal Wayne packet.
