# Approved independent implementation wave

Execute the approved parallel-units plan exactly. The two ready implementation
units have no producer/consumer dependency and no overlapping write path. Use the
runtime capabilities made available to the implementation Skill; do not invent a
provider-specific mechanism. Finish only after integrated verification and the
return-only review handoff.
