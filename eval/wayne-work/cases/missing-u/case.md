# Missing-U case

The supplied matrix is authoritative. The plan references U4, but that row is
absent from the matrix. Do not invent or restore it and do not start implementation.
Stop with `MISSING_U_ROW`, naming the plan and matrix as blocking artifacts.
