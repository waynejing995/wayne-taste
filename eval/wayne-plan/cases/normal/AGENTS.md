# Fixture rules

- During planning, write only under `docs/plans/`; do not edit code or upstream docs.
- Treat `docs/decisions/`, `docs/specs/`, and `docs/test-matrix/` as source artifacts.
- Existing active plans are constraints, not suggestions.
- Do not use gstack or any gstack-named skill.
- Use `uv run pytest` for implementation verification.
- Do not commit, push, or create branches.
