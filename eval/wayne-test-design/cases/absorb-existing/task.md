Read `/workspace/skill/SKILL.md`, its routed template, and
`/workspace/_shared/e2e-contract.md` completely. Design the approved spec's test
matrix now. The user approves writing exactly `docs/test-matrix/matrix.md`.
Do not modify the spec, implement, or run tests. Return the final matrix summary.
