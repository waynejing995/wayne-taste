# Wayne Neat eval

This exact-change harness proves that the live candidate is the creation/control
skill with only the duplicate global-owner block removed. It also proves that the
direct sync-matrix resource is unchanged and the Forge `inherits` error is gone.

```bash
uv run eval/wayne-neat/calibrate.py
```
