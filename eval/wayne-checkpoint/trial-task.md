Read `/workspace/skill/SKILL.md` and the packet template completely, then follow
the requested checkpoint handoff in `/workspace/repo`. Read `AGENTS.md` and
`case.md`; all required input is local.

Write only the checkpoint-owned `.wayne/**` state permitted by the Skill. Do not
edit product files, implement work, invoke the next stage, commit, branch, push,
publish, or use network access. Return only the concise user-visible handoff result.

The isolated trial's installed-Skill registry is
`/workspace/support/available-skills.md`; use it only to validate a caller-supplied
target. It is not a route map.

No evaluator, expected route, alternate Skill version, or hidden test is available.
